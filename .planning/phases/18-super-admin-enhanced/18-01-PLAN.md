---
phase: 18-super-admin-enhanced
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/hooks/useAdminDashboard.ts
  - frontend/src/app/admin/dashboard/page.tsx
  - frontend/src/hooks/useAdminAuditLogs.ts
  - frontend/src/app/admin/audit-logs/page.tsx
autonomous: true
requirements: [SAI-04]

must_haves:
  truths:
    - "Admin can view MRR dashboard with active subscription count, MRR in dollars, plan distribution, churn rate, and 30-day trend chart"
    - "Admin can view a paginated audit log table with timestamp, action, resource, IP, and details columns"
    - "Admin can filter audit logs by action type, tenant ID, and actor ID"
    - "Admin can paginate through audit log entries"
  artifacts:
    - path: "frontend/src/hooks/useAdminDashboard.ts"
      provides: "useAdminDashboard hook calling GET /admin/dashboard/mrr"
      exports: ["useAdminDashboard", "MRRDashboardResponse"]
    - path: "frontend/src/app/admin/dashboard/page.tsx"
      provides: "MRR dashboard page with metrics cards and Recharts trend chart"
      contains: "ResponsiveContainer"
    - path: "frontend/src/hooks/useAdminAuditLogs.ts"
      provides: "useAdminAuditLogs hook calling GET /admin/audit-logs/"
      exports: ["useAdminAuditLogs", "AuditLogItem"]
    - path: "frontend/src/app/admin/audit-logs/page.tsx"
      provides: "Paginated audit log viewer with action/tenant/actor filters"
      contains: "PAGE_LIMIT"
  key_links:
    - from: "frontend/src/hooks/useAdminDashboard.ts"
      to: "/admin/dashboard/mrr"
      via: "adminApiClient GET"
      pattern: "adminApiClient.*admin/dashboard/mrr"
    - from: "frontend/src/hooks/useAdminAuditLogs.ts"
      to: "/admin/audit-logs/"
      via: "adminApiClient GET with URLSearchParams"
      pattern: "adminApiClient.*admin/audit-logs"
    - from: "frontend/src/app/admin/dashboard/page.tsx"
      to: "recharts"
      via: "LineChart for 30-day trend"
      pattern: "LineChart|ResponsiveContainer"
---

<objective>
Install Recharts, create MRR dashboard page and audit log viewer for the super-admin portal.

Purpose: Give the admin visibility into platform revenue health (MRR metrics + 30-day trend chart) and a searchable audit trail of all sensitive actions across the platform.
Output: Two new admin pages (/admin/dashboard, /admin/audit-logs) with their TanStack Query hooks, both fully wired to existing backend endpoints.
</objective>

<execution_context>
@/Users/gilberto/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gilberto/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-super-admin-ui/17-01-SUMMARY.md
@.planning/phases/17-super-admin-ui/17-02-SUMMARY.md
@frontend/src/hooks/useAdminTenants.ts
@frontend/src/lib/admin-api-client.ts
@frontend/src/app/admin/tenants/page.tsx
@frontend/src/app/admin/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Recharts and create dashboard + audit log hooks</name>
  <files>
    frontend/src/hooks/useAdminDashboard.ts
    frontend/src/hooks/useAdminAuditLogs.ts
  </files>
  <action>
First, install recharts:
```
cd frontend && pnpm add recharts
```

Then create `frontend/src/hooks/useAdminDashboard.ts`:
- "use client" directive
- TypeScript interfaces matching backend MRRDashboardResponse exactly:
  - PlanDistributionItem: { plan_slug: string, plan_name: string, count: number }
  - MRRTrendPoint: { date: string, mrr_cents: number, active_count: number }
  - MRRDashboardResponse: { active_subscription_count: number, mrr_cents: number, plan_distribution: PlanDistributionItem[], canceled_count_30d: number, churn_rate: number, trend: MRRTrendPoint[], computed_at: string }
- ADMIN_DASHBOARD_KEYS query key factory: { mrr: () => ["admin", "dashboard", "mrr"] as const }
- useAdminDashboard hook: useQuery calling adminApiClient<MRRDashboardResponse>("/admin/dashboard/mrr"), staleTime: 60_000 (dashboard data is less volatile)
- Export all interfaces and the hook

Then create `frontend/src/hooks/useAdminAuditLogs.ts`:
- "use client" directive
- TypeScript interfaces matching backend audit/schemas.py:
  - AuditLogItem: { id: string, created_at: string, actor_id: string | null, tenant_id: string | null, action: string, resource_type: string, resource_id: string | null, ip_address: string | null, details: Record<string, unknown> }
  - AuditLogListResponse: { items: AuditLogItem[], total: number }
- ADMIN_AUDIT_KEYS query key factory: { list: (params) => ["admin", "audit-logs", params] as const }
- useAdminAuditLogs(params: { limit?: number, offset?: number, action?: string | null, tenant_id?: string | null, actor_id?: string | null }) hook:
  - Build URLSearchParams, skip null/undefined/empty values (same pattern as useAdminTenants)
  - GET /admin/audit-logs/ (trailing slash required — backend audit_router prefix is "/admin/audit-logs" and the endpoint is "/")
  - staleTime: 30_000
- Export all interfaces and the hook

Follow the exact patterns from useAdminTenants.ts (URLSearchParams building, query key factory, adminApiClient import).
  </action>
  <verify>
Run `cd /Users/gilberto/projetos/wxk/wxcode-adm/frontend && npx tsc --noEmit` — zero type errors.
  </verify>
  <done>
Both hook files exist, export correct TypeScript interfaces and TanStack Query hooks, and type-check cleanly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create MRR dashboard page and audit log viewer page</name>
  <files>
    frontend/src/app/admin/dashboard/page.tsx
    frontend/src/app/admin/audit-logs/page.tsx
  </files>
  <action>
Create `frontend/src/app/admin/dashboard/page.tsx`:
- "use client" directive
- Import useAdminDashboard and all its types
- Import Recharts: { ResponsiveContainer, LineChart, Line, XAxis, YAxis, Tooltip, CartesianGrid } from "recharts"
- Import AdminNav pattern: copy the inline AdminNav from tenants/page.tsx but with 4 nav links:
  - Dashboard (/admin/dashboard) — active (cyan-400 + border-b-2)
  - Tenants (/admin/tenants)
  - Users (/admin/users)
  - Audit Logs (/admin/audit-logs)
  - Logout button on the right
- Page structure:
  1. AdminNav with Dashboard active
  2. Page header: "Revenue Dashboard" + subtitle "Monthly recurring revenue and subscription metrics."
  3. Loading state: LoadingSkeleton lines={8} in a zinc-900/50 card
  4. Error state: ErrorState with retry
  5. 4 metric cards in a 2x2 grid (lg:grid-cols-4, sm:grid-cols-2):
     - "Active Subscriptions": data.active_subscription_count (large number, emerald-400 icon)
     - "Monthly Revenue": format mrr_cents as dollars (mrr_cents / 100).toLocaleString('en-US', { style: 'currency', currency: 'USD' }) (large number, cyan-400)
     - "Churn Rate": (data.churn_rate * 100).toFixed(1) + "%" (amber-400 if > 5%, emerald-400 if <= 5%)
     - "Canceled (30d)": data.canceled_count_30d (rose-400)
  - Card styling: rounded-lg border border-zinc-800 bg-zinc-900/50 p-6
  6. 30-day MRR Trend chart section:
     - Title: "MRR Trend (30 days)"
     - ResponsiveContainer width="100%" height={300}
     - LineChart with data={data.trend.map(p => ({ ...p, mrr_dollars: p.mrr_cents / 100 }))}
     - XAxis dataKey="date" with tick formatter showing "MMM dd" (new Date(val).toLocaleDateString(undefined, { month: 'short', day: 'numeric' }))
     - YAxis with $ prefix formatter
     - Line dataKey="mrr_dollars" stroke="#22d3ee" (cyan-400) strokeWidth={2} dot={false}
     - CartesianGrid strokeDasharray="3 3" stroke="#3f3f46" (zinc-700)
     - Tooltip with dark styling: contentStyle={{ backgroundColor: '#18181b', border: '1px solid #3f3f46', borderRadius: '8px' }}, labelStyle={{ color: '#a1a1aa' }}
  7. Plan Distribution section:
     - Title: "Plan Distribution"
     - Horizontal bar-like display: for each plan in data.plan_distribution, show plan_name + count in a row with a proportional bg bar
     - Each row: plan_name on left, count badge on right, with a zinc-800 bg bar proportional to count/max_count

Create `frontend/src/app/admin/audit-logs/page.tsx`:
- "use client" directive
- Import useAdminAuditLogs and types
- Same AdminNav pattern but with "Audit Logs" active (cyan-400 + border-b-2)
- Page structure:
  1. AdminNav with Audit Logs active
  2. Page header: "Audit Logs" + subtitle "Platform-wide audit trail of sensitive actions."
  3. Filter bar (3 GlowInput fields in a flex row):
     - Action type: GlowInput with placeholder "Filter by action..."
     - Tenant ID: GlowInput with placeholder "Filter by tenant ID..."
     - Actor ID: GlowInput with placeholder "Filter by actor ID..."
     - All filters reset page to 0 on change
  4. Loading state: LoadingSkeleton lines={8}
  5. Error state: ErrorState with retry
  6. Empty state: EmptyState title="No audit logs found"
  7. Table with columns: Timestamp, Action, Resource, Actor, Tenant, IP, Details
     - Timestamp: formatDate helper showing date + time (toLocaleString)
     - Action: badge with the action string (text-cyan-400 bg-cyan-400/10 rounded-full px-2 py-0.5)
     - Resource: resource_type + (resource_id ? ": " + resource_id.slice(0,8) + "..." : "")
     - Actor: actor_id ? actor_id.slice(0,8) + "..." : "System" (monospace, truncated UUID)
     - Tenant: tenant_id ? tenant_id.slice(0,8) + "..." : "—" (monospace, truncated UUID)
     - IP: ip_address ?? "—"
     - Details: JSON.stringify details truncated to 50 chars with "..." if longer; show full on hover via title attribute
  8. Pagination: same pattern as tenants page — PAGE_LIMIT=50, page/offset state, "Showing X-Y of Z entries", Previous/Next GlowButton ghost
  - Table styling: same as tenants page (rounded-lg border border-zinc-800, thead bg-zinc-900/50, etc.)

IMPORTANT for both pages: The AdminNav must include all 4 links (Dashboard, Tenants, Users, Audit Logs) with the current page highlighted. Use the same nav styling pattern from tenants/page.tsx but extend with 4 items instead of 2.
  </action>
  <verify>
Run `cd /Users/gilberto/projetos/wxk/wxcode-adm/frontend && npx tsc --noEmit` — zero type errors. Visually confirm both page files use correct Recharts imports and follow established admin page patterns.
  </verify>
  <done>
/admin/dashboard renders 4 metric cards, a 30-day MRR Recharts line chart, and plan distribution. /admin/audit-logs renders a paginated table with action/tenant/actor filters. Both pages include the extended 4-link AdminNav.
  </done>
</task>

</tasks>

<verification>
1. `cd frontend && npx tsc --noEmit` passes with zero errors
2. `cd frontend && pnpm build` completes without errors
3. Recharts is listed in frontend/package.json dependencies
4. All 4 new files exist and import from adminApiClient (not apiClient)
5. Dashboard page uses Recharts LineChart for trend visualization
6. Audit log page supports action, tenant_id, actor_id filters
7. Both pages include AdminNav with 4 navigation links
</verification>

<success_criteria>
- MRR dashboard page at /admin/dashboard shows metrics cards, 30-day trend chart, and plan distribution
- Audit log viewer at /admin/audit-logs shows paginated table with 3 filter inputs
- Both hooks call the correct backend endpoints via adminApiClient
- Recharts installed and rendering line chart for MRR trend
- AdminNav extended to include Dashboard and Audit Logs links
</success_criteria>

<output>
After completion, create `.planning/phases/18-super-admin-enhanced/18-01-SUMMARY.md`
</output>
