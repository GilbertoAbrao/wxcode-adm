---
phase: 21-plan-limits-extension
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/wxcode_adm/billing/models.py
  - backend/src/wxcode_adm/billing/schemas.py
  - backend/src/wxcode_adm/billing/service.py
  - backend/alembic/versions/009_add_plan_limits_fields.py
  - backend/tests/test_billing.py
autonomous: true
requirements:
  - PLAN-LIMITS-01
  - PLAN-LIMITS-02
  - PLAN-LIMITS-03
  - PLAN-LIMITS-04
  - PLAN-LIMITS-05

must_haves:
  truths:
    - "Plan model has max_projects, max_output_projects, max_storage_gb integer fields with defaults"
    - "Admin can create a plan with wxcode limit fields via POST /admin/billing/plans"
    - "Admin can update wxcode limit fields via PATCH /admin/billing/plans/{id}"
    - "Plan response includes the 3 new limit fields"
    - "Migration 009 adds columns to plans table with server_default for existing rows"
  artifacts:
    - path: "backend/src/wxcode_adm/billing/models.py"
      provides: "Plan model with max_projects, max_output_projects, max_storage_gb"
      contains: "max_projects"
    - path: "backend/src/wxcode_adm/billing/schemas.py"
      provides: "CreatePlanRequest, UpdatePlanRequest, PlanResponse with new fields"
      contains: "max_storage_gb"
    - path: "backend/src/wxcode_adm/billing/service.py"
      provides: "create_plan and update_plan handling new fields"
      contains: "max_projects"
    - path: "backend/alembic/versions/009_add_plan_limits_fields.py"
      provides: "Alembic migration adding 3 columns to plans table"
      contains: "revision.*009"
    - path: "backend/tests/test_billing.py"
      provides: "Tests covering plan CRUD with new limit fields"
      contains: "max_projects"
  key_links:
    - from: "backend/src/wxcode_adm/billing/schemas.py"
      to: "backend/src/wxcode_adm/billing/models.py"
      via: "Pydantic schema fields match ORM model columns"
      pattern: "max_projects.*max_output_projects.*max_storage_gb"
    - from: "backend/src/wxcode_adm/billing/service.py"
      to: "backend/src/wxcode_adm/billing/models.py"
      via: "create_plan() passes new fields to Plan() constructor"
      pattern: "max_projects=body\\.max_projects"
---

<objective>
Add wxcode operational limit fields (max_projects, max_output_projects, max_storage_gb) to the Plan model, update Pydantic schemas, admin CRUD service, create migration 009, and write tests.

Purpose: Phase 23 (Admin UI - Claude Management) needs these plan limits exposed in the admin plan form. The wxcode engine will use these limits for per-tenant enforcement.
Output: Extended Plan model with 3 new integer fields, migration 009, updated schemas/service, tests passing.
</objective>

<execution_context>
@/Users/gilberto/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gilberto/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/src/wxcode_adm/billing/models.py
@backend/src/wxcode_adm/billing/schemas.py
@backend/src/wxcode_adm/billing/service.py
@backend/src/wxcode_adm/billing/router.py
@backend/alembic/versions/008_add_claude_wxcode_tenant_fields.py
@backend/tests/test_billing.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Plan limit fields, migration 009, update schemas and service</name>
  <files>
    backend/src/wxcode_adm/billing/models.py
    backend/src/wxcode_adm/billing/schemas.py
    backend/src/wxcode_adm/billing/service.py
    backend/alembic/versions/009_add_plan_limits_fields.py
  </files>
  <action>
1. **Plan model** (`backend/src/wxcode_adm/billing/models.py`):
   Add 3 new fields to the `Plan` class, after the existing `member_cap` field and before `is_active`:
   ```python
   max_projects: Mapped[int] = mapped_column(
       Integer,
       nullable=False,
       default=5,
   )
   max_output_projects: Mapped[int] = mapped_column(
       Integer,
       nullable=False,
       default=20,
   )
   max_storage_gb: Mapped[int] = mapped_column(
       Integer,
       nullable=False,
       default=10,
   )
   ```
   Follow the exact `mapped_column()` pattern used by `member_cap` and `token_quota`.

2. **Pydantic schemas** (`backend/src/wxcode_adm/billing/schemas.py`):
   - `CreatePlanRequest`: add `max_projects: int = Field(ge=1, default=5)`, `max_output_projects: int = Field(ge=1, default=20)`, `max_storage_gb: int = Field(ge=1, default=10)` after `member_cap`.
   - `UpdatePlanRequest`: add `max_projects: Optional[int] = Field(default=None, ge=1)`, `max_output_projects: Optional[int] = Field(default=None, ge=1)`, `max_storage_gb: Optional[int] = Field(default=None, ge=1)` after `member_cap`.
   - `PlanResponse`: add `max_projects: int`, `max_output_projects: int`, `max_storage_gb: int` after `member_cap`.

3. **Service layer** (`backend/src/wxcode_adm/billing/service.py`):
   - In `create_plan()`, add the 3 new fields to the `Plan(...)` constructor call:
     ```python
     max_projects=body.max_projects,
     max_output_projects=body.max_output_projects,
     max_storage_gb=body.max_storage_gb,
     ```
   - In `update_plan()`, add 3 new `if body.X is not None` blocks (same pattern as `member_cap`):
     ```python
     if body.max_projects is not None:
         plan.max_projects = body.max_projects
     if body.max_output_projects is not None:
         plan.max_output_projects = body.max_output_projects
     if body.max_storage_gb is not None:
         plan.max_storage_gb = body.max_storage_gb
     ```
     Place these after the `member_cap` block and before the `is_active` block. These fields do NOT trigger Stripe price re-sync (they are wxcode-only limits, not billing amounts).

4. **Alembic migration 009** (`backend/alembic/versions/009_add_plan_limits_fields.py`):
   - `revision = "009"`, `down_revision = "008"`
   - Follow exact pattern of migration 008: use `op.add_column("plans", ...)` with `server_default` for non-nullable integer columns so existing rows get defaults.
   - Three `op.add_column` calls:
     - `max_projects`, Integer, nullable=False, server_default=sa.text("5")
     - `max_output_projects`, Integer, nullable=False, server_default=sa.text("20")
     - `max_storage_gb`, Integer, nullable=False, server_default=sa.text("10")
   - `downgrade()` drops the 3 columns in reverse order.
   - Include docstring explaining Phase 21 purpose.
  </action>
  <verify>
    Run `cd /Users/gilberto/projetos/wxk/wxcode-adm/backend && python -c "from wxcode_adm.billing.models import Plan; print([c.name for c in Plan.__table__.columns if 'max' in c.name or 'storage' in c.name])"` to confirm model columns exist.
    Run `cd /Users/gilberto/projetos/wxk/wxcode-adm/backend && python -c "from wxcode_adm.billing.schemas import CreatePlanRequest, PlanResponse; print(list(CreatePlanRequest.model_fields.keys())); print(list(PlanResponse.model_fields.keys()))"` to confirm schema fields.
  </verify>
  <done>
    Plan model has max_projects (default 5), max_output_projects (default 20), max_storage_gb (default 10). Schemas include these fields in create/update/response. Service passes them through. Migration 009 exists with server_default for existing rows.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add tests for plan limits CRUD</name>
  <files>
    backend/tests/test_billing.py
  </files>
  <action>
Add 3 new test functions to `backend/tests/test_billing.py`, using the existing helper functions (`_seed_super_admin`, `_admin_login`). Place them in the SC1 section after `test_list_active_plans`.

1. **test_create_plan_with_limits** — Create a plan via POST /admin/billing/plans with explicit limit values (max_projects=10, max_output_projects=50, max_storage_gb=25). Assert response contains the correct values.

2. **test_create_plan_limits_defaults** — Create a plan via POST /admin/billing/plans WITHOUT specifying limit fields. Assert response contains the defaults: max_projects=5, max_output_projects=20, max_storage_gb=10.

3. **test_update_plan_limits** — Create a plan, then PATCH it with new limit values (max_projects=15, max_storage_gb=50). Assert response reflects the updated values. Assert max_output_projects remains unchanged (was not in PATCH body).

Each test follows the existing pattern:
- Uses unique admin email per test (e.g., `admin_limits@test.com`)
- Calls `_seed_super_admin` + `_admin_login` for admin-audience JWT
- Uses `async def test_xxx(client):` with `c, redis, app, test_db = client` destructuring
- Asserts on response status code and JSON body fields
  </action>
  <verify>
    Run `cd /Users/gilberto/projetos/wxk/wxcode-adm/backend && python -m pytest tests/test_billing.py -x -v -k "limits" 2>&1 | tail -20` to verify new tests pass.
    Run `cd /Users/gilberto/projetos/wxk/wxcode-adm/backend && python -m pytest tests/test_billing.py -x -v 2>&1 | tail -5` to verify all existing billing tests still pass.
  </verify>
  <done>
    3 new tests pass: create with explicit limits, create with defaults, update limits. All existing billing tests still pass (no regressions).
  </done>
</task>

</tasks>

<verification>
1. `python -m pytest tests/test_billing.py -x -v` — all tests pass (existing + 3 new)
2. `python -c "from wxcode_adm.billing.models import Plan; ..."` — model has new columns
3. `python -c "from wxcode_adm.billing.schemas import PlanResponse; ..."` — response includes new fields
4. Migration 009 file exists with correct revision chain (008 -> 009)
</verification>

<success_criteria>
- Plan model has 3 new integer fields with correct defaults
- Migration 009 adds columns with server_default for existing rows
- CreatePlanRequest accepts optional limit fields with defaults
- UpdatePlanRequest accepts optional limit fields for partial update
- PlanResponse includes all 3 limit fields
- create_plan service passes limits to Plan constructor
- update_plan service applies non-None limit fields
- 3 new tests pass, 0 existing tests broken
</success_criteria>

<output>
After completion, create `.planning/phases/21-plan-limits-extension/21-01-SUMMARY.md`
</output>
