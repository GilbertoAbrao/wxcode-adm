---
phase: 05-platform-security
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/wxcode_adm/common/mail.py
  - backend/src/wxcode_adm/templates/email/base.html
  - backend/src/wxcode_adm/templates/email/verify_email.html
  - backend/src/wxcode_adm/templates/email/verify_email.txt
  - backend/src/wxcode_adm/templates/email/reset_password.html
  - backend/src/wxcode_adm/templates/email/reset_password.txt
  - backend/src/wxcode_adm/templates/email/invitation.html
  - backend/src/wxcode_adm/templates/email/invitation.txt
  - backend/src/wxcode_adm/templates/email/payment_failed.html
  - backend/src/wxcode_adm/templates/email/payment_failed.txt
  - backend/src/wxcode_adm/auth/email.py
  - backend/src/wxcode_adm/tenants/email.py
  - backend/src/wxcode_adm/billing/email.py
autonomous: true
requirements:
  - PLAT-05

must_haves:
  truths:
    - "Email verification sends branded HTML email with OTP code in a styled code block, plus a plain-text fallback"
    - "Password reset sends branded HTML email with a CTA button linking to the reset URL, plus a plain-text fallback"
    - "Tenant invitation sends branded HTML email with workspace name, role, and a CTA button to accept, plus a plain-text fallback"
    - "Payment failure sends branded HTML email with workspace name and warning context, plus a plain-text fallback"
    - "All 4 HTML templates extend a shared base.html with WXCODE header, content block, and footer"
    - "FastMail singleton is constructed once (not per-send) via common/mail.py"
  artifacts:
    - path: "backend/src/wxcode_adm/common/mail.py"
      provides: "Shared FastMail singleton with TEMPLATE_FOLDER configured"
      contains: "fast_mail = FastMail"
    - path: "backend/src/wxcode_adm/templates/email/base.html"
      provides: "Jinja2 base layout with WXCODE branded header, content block, footer"
      contains: "block content"
    - path: "backend/src/wxcode_adm/auth/email.py"
      provides: "Refactored send_verification_email and send_reset_email using html_template + plain_template"
      contains: "html_template"
    - path: "backend/src/wxcode_adm/tenants/email.py"
      provides: "Refactored send_invitation_email using html_template + plain_template"
      contains: "html_template"
    - path: "backend/src/wxcode_adm/billing/email.py"
      provides: "Refactored send_payment_failed_email using html_template + plain_template"
      contains: "html_template"
  key_links:
    - from: "backend/src/wxcode_adm/common/mail.py"
      to: "backend/src/wxcode_adm/templates/email/"
      via: "TEMPLATE_FOLDER points to templates/email directory"
      pattern: "TEMPLATE_FOLDER"
    - from: "backend/src/wxcode_adm/auth/email.py"
      to: "backend/src/wxcode_adm/common/mail.py"
      via: "imports fast_mail singleton"
      pattern: "from wxcode_adm\\.common\\.mail import fast_mail"
    - from: "backend/src/wxcode_adm/templates/email/verify_email.html"
      to: "backend/src/wxcode_adm/templates/email/base.html"
      via: "Jinja2 extends inheritance"
      pattern: "extends.*base\\.html"
---

<objective>
Create branded HTML email templates for all 4 transactional emails (verify, reset, invite, payment_failed) with a shared Jinja2 base layout, refactor all existing plain-text senders to use the shared FastMail singleton with html_template + plain_template support.

Purpose: Replace plain-text email stubs with polished, branded HTML+plain-text multipart emails for professional communication with users (PLAT-05).

Output: 9 template files (1 base + 4 HTML + 4 plain-text); shared FastMail singleton in common/mail.py; all 4 email sender functions refactored.
</objective>

<execution_context>
@/Users/gilberto/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gilberto/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-platform-security/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create FastMail singleton and all 9 email template files</name>
  <files>
    backend/src/wxcode_adm/common/mail.py
    backend/src/wxcode_adm/templates/email/base.html
    backend/src/wxcode_adm/templates/email/verify_email.html
    backend/src/wxcode_adm/templates/email/verify_email.txt
    backend/src/wxcode_adm/templates/email/reset_password.html
    backend/src/wxcode_adm/templates/email/reset_password.txt
    backend/src/wxcode_adm/templates/email/invitation.html
    backend/src/wxcode_adm/templates/email/invitation.txt
    backend/src/wxcode_adm/templates/email/payment_failed.html
    backend/src/wxcode_adm/templates/email/payment_failed.txt
  </files>
  <action>
1. **common/mail.py** — Create shared FastMail singleton:
   ```python
   from pathlib import Path
   from fastapi_mail import ConnectionConfig, FastMail
   from wxcode_adm.config import settings

   _mail_conf = ConnectionConfig(
       MAIL_USERNAME=settings.SMTP_USER,
       MAIL_PASSWORD=settings.SMTP_PASSWORD,
       MAIL_FROM=settings.SMTP_FROM_EMAIL,
       MAIL_FROM_NAME=settings.SMTP_FROM_NAME,
       MAIL_PORT=settings.SMTP_PORT,
       MAIL_SERVER=settings.SMTP_HOST,
       MAIL_STARTTLS=settings.SMTP_TLS,
       MAIL_SSL_TLS=settings.SMTP_SSL,
       USE_CREDENTIALS=bool(settings.SMTP_USER),
       TEMPLATE_FOLDER=Path(__file__).parent.parent / "templates" / "email",
   )
   fast_mail = FastMail(_mail_conf)
   ```

2. **templates/email/base.html** — Branded Jinja2 base layout using WXCODE design tokens:
   - Table-based layout (email-client compatible, NO flexbox/grid/CSS variables)
   - ALL CSS inline (email clients strip `<style>` blocks)
   - Outer background: #f4f4f5 (obsidian-100)
   - Header bar: #18181b background, "WXCODE" wordmark in #f4f4f5, bold, letter-spacing: 2px
   - Content area: #ffffff background, 600px max width, 8px border-radius, padding 40px
   - Content text: #27272a (obsidian-700), 15px, line-height 1.6
   - Footer: #f4f4f5 background, #71717a text, 12px, "(c) 2026 WXCODE. All rights reserved."
   - `{% block title %}WXCODE{% endblock %}` in `<title>`
   - `{% block content %}{% endblock %}` in content area

3. **templates/email/verify_email.html** — Extends base.html:
   - Title block: "Verify your email -- WXCODE"
   - Heading: "Verify your email address" (h2, #18181b, 20px)
   - Body: "Use the code below to verify your email. It expires in **10 minutes**."
   - OTP display: centered div, #f4f4f5 background, 6px border-radius, 32px font, bold, letter-spacing 8px, #18181b color, 24px padding: `{{ code }}`
   - Footer note: "If you did not create a WXCODE account, ignore this email." (#71717a, 13px)

4. **templates/email/verify_email.txt** — Plain text:
   ```
   WXCODE - Verify your email

   Your verification code is: {{ code }}

   This code expires in 10 minutes.

   If you did not create a WXCODE account, ignore this email.
   ```

5. **templates/email/reset_password.html** — Extends base.html:
   - Title: "Reset your password -- WXCODE"
   - Heading: "Reset your password"
   - Body: "We received a request to reset your password. Click the button below to choose a new one."
   - CTA button: `<a>` styled as table-cell button, #3b82f6 background, white text, bold, 14px, 14px 32px padding, 6px border-radius, text-decoration none, display inline-block: "Reset Password" linking to `{{ reset_link }}`
   - Fallback link text: "Or copy and paste this link: {{ reset_link }}" (13px, #71717a)
   - Expiry note: "This link expires in 24 hours and can only be used once."
   - Ignore note: "If you did not request a password reset, ignore this email."

6. **templates/email/reset_password.txt** — Plain text with reset_link.

7. **templates/email/invitation.html** — Extends base.html:
   - Title: "You're invited to {{ tenant_name }} -- WXCODE"
   - Heading: "You've been invited!"
   - Body: "You've been invited to join **{{ tenant_name }}** as **{{ role }}**."
   - CTA button: "Accept Invitation" linking to `{{ invite_link }}` (same style as reset)
   - Fallback link text
   - Expiry: "This invitation expires in 7 days."

8. **templates/email/invitation.txt** — Plain text with tenant_name, role, invite_link.

9. **templates/email/payment_failed.html** — Extends base.html:
   - Title: "Payment failed -- WXCODE"
   - Heading: "Payment failed" (use #f59e0b amber accent on a small warning icon or label)
   - Body: "Payment has failed for your workspace **{{ tenant_name }}**."
   - Explain: "Your workspace access to the wxcode engine has been restricted. Please update your payment method to restore access."
   - CTA button: "Update Payment Method" linking to `{{ billing_url }}` (use #3b82f6 blue, not red — keep action positive)
   - Note: "You can still access wxcode-adm to manage your account and billing."

10. **templates/email/payment_failed.txt** — Plain text with tenant_name.

CRITICAL TEMPLATE RULES (from research):
- Use `{{ variable }}` directly, NOT `{{ body.variable }}` — fastapi-mail 1.6.2 passes template_body variables at top level.
- ALL CSS must be inline (`style="..."`) — no `<style>` blocks.
- Use `<table>` layout, not flexbox/grid.
  </action>
  <verify>
    - `ls backend/src/wxcode_adm/templates/email/` shows 9 files (base.html + 4 .html + 4 .txt)
    - `python -c "from wxcode_adm.common.mail import fast_mail; print(type(fast_mail))"` succeeds
    - `grep -c "block content" backend/src/wxcode_adm/templates/email/base.html` returns 1
    - `grep -c "extends" backend/src/wxcode_adm/templates/email/verify_email.html` returns 1
    - No template uses `body.` prefix: `grep -rL "body\." backend/src/wxcode_adm/templates/email/*.html` shows all files (or `grep -c "{{ body\." ...` returns 0)
  </verify>
  <done>FastMail singleton created with TEMPLATE_FOLDER; 9 branded email templates created (1 base + 4 HTML extending base + 4 plain-text); WXCODE design tokens applied; all templates use `{{ variable }}` syntax.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor all 4 email sender functions to use templates</name>
  <files>
    backend/src/wxcode_adm/auth/email.py
    backend/src/wxcode_adm/tenants/email.py
    backend/src/wxcode_adm/billing/email.py
  </files>
  <action>
1. **auth/email.py** — Refactor both functions:

   a) `send_verification_email(ctx, user_id, email, code)`:
      - Keep the `logger.info(f"[DEV] Verification code for {email}: {code}")` line (essential for dev/test)
      - Replace the inline ConnectionConfig + plain-text MessageSchema with:
        ```python
        from fastapi_mail import MessageSchema, MessageType
        from wxcode_adm.common.mail import fast_mail

        message = MessageSchema(
            subject="WXCODE — Verify your email",
            recipients=[email],
            template_body={"code": code, "email": email},
            subtype=MessageType.html,
        )
        await fast_mail.send_message(
            message,
            html_template="verify_email.html",
            plain_template="verify_email.txt",
        )
        ```
      - Keep the try/except wrapper so the job never fails on SMTP errors
      - Remove the per-call ConnectionConfig and FastMail construction

   b) `send_reset_email(ctx, user_id, email, reset_link)`:
      - Same pattern: use fast_mail singleton, template_body={"reset_link": reset_link, "email": email}
      - html_template="reset_password.html", plain_template="reset_password.txt"
      - Subject: "WXCODE — Reset your password"

2. **tenants/email.py** — Refactor `send_invitation_email(ctx, email, tenant_name, invite_link, role)`:
   - Use fast_mail singleton
   - template_body={"tenant_name": tenant_name, "invite_link": invite_link, "role": role, "email": email}
   - html_template="invitation.html", plain_template="invitation.txt"
   - Subject: f"You've been invited to {tenant_name} — WXCODE"

3. **billing/email.py** — Refactor `send_payment_failed_email(ctx, email, tenant_name)`:
   - Use fast_mail singleton
   - template_body={"tenant_name": tenant_name, "email": email, "billing_url": f"{settings.FRONTEND_URL}/billing"}
   - html_template="payment_failed.html", plain_template="payment_failed.txt"
   - Subject: f"[WXCODE] Payment failed for {tenant_name}"

ALL REFACTORED FUNCTIONS must:
- Keep the `[DEV] logger.info(...)` line at the top (for dev/test visibility without SMTP)
- Keep the `try/except Exception` wrapper around the SMTP send (graceful degradation)
- Use `from wxcode_adm.common.mail import fast_mail` (lazy import inside the try block to avoid import errors when SMTP is not configured)
- Remove the per-call `ConnectionConfig` and `FastMail(conf)` construction — use the shared singleton
- Function signatures MUST NOT change (they are registered as arq jobs in worker.py)
  </action>
  <verify>
    - `grep -c "html_template" backend/src/wxcode_adm/auth/email.py` returns 2 (verify + reset)
    - `grep -c "html_template" backend/src/wxcode_adm/tenants/email.py` returns 1
    - `grep -c "html_template" backend/src/wxcode_adm/billing/email.py` returns 1
    - `grep -c "ConnectionConfig" backend/src/wxcode_adm/auth/email.py` returns 0 (removed per-call construction)
    - `grep -c "ConnectionConfig" backend/src/wxcode_adm/tenants/email.py` returns 0
    - `grep -c "ConnectionConfig" backend/src/wxcode_adm/billing/email.py` returns 0
    - `grep -c "\\[DEV\\]" backend/src/wxcode_adm/auth/email.py` returns 2 (dev logging preserved)
  </verify>
  <done>All 4 email sender functions refactored to use shared FastMail singleton with HTML+plain-text template pairs; per-call ConnectionConfig construction eliminated; function signatures preserved for arq compatibility; dev logging preserved.</done>
</task>

</tasks>

<verification>
1. FastMail singleton: `python -c "from wxcode_adm.common.mail import fast_mail; print('OK')"`
2. Templates exist: `ls -1 backend/src/wxcode_adm/templates/email/ | wc -l` returns 9
3. All HTML templates extend base: `grep -l "extends" backend/src/wxcode_adm/templates/email/*.html | wc -l` returns 4
4. No per-call ConnectionConfig: `grep -rn "ConnectionConfig" backend/src/wxcode_adm/auth/email.py backend/src/wxcode_adm/tenants/email.py backend/src/wxcode_adm/billing/email.py` returns 0 lines
5. All senders use html_template: `grep -rn "html_template" backend/src/wxcode_adm/auth/email.py backend/src/wxcode_adm/tenants/email.py backend/src/wxcode_adm/billing/email.py` returns 4 matches
</verification>

<success_criteria>
- 9 template files exist in templates/email/ (1 base + 4 HTML + 4 plain-text)
- All HTML templates use table-based layout with inline CSS
- WXCODE brand colors applied (header #18181b, CTA #3b82f6, body #27272a, bg #f4f4f5)
- All 4 senders use shared FastMail singleton (no per-call ConnectionConfig)
- All 4 senders produce multipart HTML+plain-text emails
- Templates use `{{ variable }}` (not `{{ body.variable }}`)
- Function signatures unchanged (arq worker compatibility)
- Dev logging lines preserved
</success_criteria>

<output>
After completion, create `.planning/phases/05-platform-security/05-03-SUMMARY.md`
</output>
