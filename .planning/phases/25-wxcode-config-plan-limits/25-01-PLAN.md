---
phase: 25-wxcode-config-plan-limits
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/wxcode_adm/tenants/router.py
  - backend/tests/test_claude_provisioning.py
  - docs/INTEGRATION-CONTRACT.md
autonomous: true
requirements: [MISSING-01, FLOW-BREAK-01]
gap_closure: true

must_haves:
  truths:
    - "GET /tenants/{id}/wxcode-config returns plan limit fields (max_projects, max_output_projects, max_storage_gb, token_quota_5h, token_quota_weekly) when tenant has a subscription"
    - "GET /tenants/{id}/wxcode-config returns null plan_limits when tenant has no subscription"
    - "INTEGRATION-CONTRACT.md documents the plan_limits object in the wxcode-config response"
  artifacts:
    - path: "backend/src/wxcode_adm/tenants/router.py"
      provides: "wxcode-config endpoint with plan limits via TenantSubscription join"
      contains: "plan_limits"
    - path: "backend/tests/test_claude_provisioning.py"
      provides: "Tests for plan limits in wxcode-config response"
      contains: "test_wxcode_config_plan_limits"
    - path: "docs/INTEGRATION-CONTRACT.md"
      provides: "Updated contract documenting plan_limits in wxcode-config"
      contains: "plan_limits"
  key_links:
    - from: "backend/src/wxcode_adm/tenants/router.py"
      to: "TenantSubscription -> Plan"
      via: "SQLAlchemy select + where on tenant_id"
      pattern: "select.*TenantSubscription.*tenant_id"
    - from: "docs/INTEGRATION-CONTRACT.md"
      to: "backend/src/wxcode_adm/tenants/router.py"
      via: "API contract documentation"
      pattern: "plan_limits"
---

<objective>
Expose plan limits (max_projects, max_output_projects, max_storage_gb, token_quota_5h, token_quota_weekly) in the GET /tenants/{id}/wxcode-config endpoint by querying TenantSubscription -> Plan for the tenant, and update INTEGRATION-CONTRACT.md to document the new fields.

Purpose: Close gap MISSING-01 (plan limits not exposed in wxcode-config) and FLOW-BREAK-01 (wxcode engine cannot get tenant plan limits from wxcode-adm). The wxcode engine needs these limits to enforce per-tenant operational constraints (project count, storage, token quotas).

Output: Updated wxcode-config endpoint returning plan_limits object, tests proving it works, and updated integration contract.
</objective>

<execution_context>
@/Users/gilberto/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gilberto/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-claude-provisioning-api/22-02-SUMMARY.md
@.planning/phases/21-plan-limits-extension/21-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add plan limits to wxcode-config endpoint + tests</name>
  <files>backend/src/wxcode_adm/tenants/router.py, backend/tests/test_claude_provisioning.py</files>
  <action>
Modify the `get_wxcode_config` endpoint in `backend/src/wxcode_adm/tenants/router.py` (around line 715) to query the tenant's plan limits via TenantSubscription -> Plan join.

**Endpoint changes:**

1. Add imports at the top of the wxcode-config section:
   ```python
   from sqlalchemy import select
   from wxcode_adm.billing.models import TenantSubscription
   ```

2. After the tenant mismatch check (line 737), add a query to load the tenant's subscription + plan:
   ```python
   sub_result = await db.execute(
       select(TenantSubscription).where(TenantSubscription.tenant_id == tenant.id)
   )
   subscription = sub_result.scalar_one_or_none()
   ```

   Note: TenantSubscription has `plan: Mapped["Plan"] = relationship(lazy="joined")`, so `subscription.plan` is automatically loaded — no separate Plan query needed.

3. Build plan_limits dict conditionally:
   ```python
   plan_limits = None
   if subscription is not None and subscription.plan is not None:
       plan = subscription.plan
       plan_limits = {
           "max_projects": plan.max_projects,
           "max_output_projects": plan.max_output_projects,
           "max_storage_gb": plan.max_storage_gb,
           "token_quota_5h": plan.token_quota_5h,
           "token_quota_weekly": plan.token_quota_weekly,
       }
   ```

4. Add `"plan_limits": plan_limits` to the return dict (after `"max_concurrent_sessions"`).

**Test changes in `backend/tests/test_claude_provisioning.py`:**

5. Add 2 new tests after the existing wxcode-config tests section (around line 650):

   **test_wxcode_config_plan_limits_with_subscription:**
   - Sign up user, create workspace (this creates a free subscription via bootstrap_free_subscription)
   - The conftest already seeds a "Free" plan with token_quota_5h=10000, token_quota_weekly=50000, max_projects/max_output_projects/max_storage_gb at defaults (5, 20, 10)
   - GET wxcode-config and assert `plan_limits` is not None
   - Assert `plan_limits["max_projects"] == 5`
   - Assert `plan_limits["max_output_projects"] == 20`
   - Assert `plan_limits["max_storage_gb"] == 10`
   - Assert `plan_limits["token_quota_5h"] == 10000`
   - Assert `plan_limits["token_quota_weekly"] == 50000`

   **test_wxcode_config_plan_limits_no_subscription:**
   - Create a tenant directly via `_create_tenant_in_db` (no subscription)
   - Sign up a user, add them as OWNER membership directly in DB (same pattern as test_wxcode_config_viewer_denied but with OWNER role)
   - GET wxcode-config and assert `plan_limits` is None

6. Update the existing `test_wxcode_config_developer_access` test to also assert that `"plan_limits"` key is present in the response data (add `assert "plan_limits" in data`). The workspace creation flow calls bootstrap_free_subscription which creates a TenantSubscription, so plan_limits should be non-None here.

**Important patterns to follow:**
- Use `_patch_crypto_key(monkeypatch)` at the start of each test
- Use `c, redis, app, test_db = client` destructuring
- Use `_signup_verify_login` helper for user creation
- Import `select` from sqlalchemy (already imported in the test file)
- Import `TenantMembership`, `MemberRole`, `Tenant` (already imported in the test file)
  </action>
  <verify>
Run tests: `cd /Users/gilberto/projetos/wxk/wxcode-adm/backend && /opt/homebrew/bin/python3.11 -m pytest tests/test_claude_provisioning.py -v`

All tests pass including the 2 new plan_limits tests. Existing 14 tests still pass.
  </verify>
  <done>
GET /tenants/{id}/wxcode-config returns plan_limits object with 5 fields when tenant has subscription, and plan_limits=null when no subscription. All 16 tests pass (14 existing + 2 new).
  </done>
</task>

<task type="auto">
  <name>Task 2: Update INTEGRATION-CONTRACT.md with plan_limits documentation</name>
  <files>docs/INTEGRATION-CONTRACT.md</files>
  <action>
Update `docs/INTEGRATION-CONTRACT.md` Section 4 (Configuration Endpoint) to document the new `plan_limits` field in the wxcode-config response.

1. In the Response (200) JSON example (around line 112), add after `"max_concurrent_sessions": 5`:
   ```json
   "plan_limits": {
     "max_projects": 5,
     "max_output_projects": 20,
     "max_storage_gb": 10,
     "token_quota_5h": 10000,
     "token_quota_weekly": 50000
   }
   ```

2. In the response field table (after the `max_concurrent_sessions` row), add:

   | `plan_limits`                      | object or null  | Operational limits from the tenant's billing plan; null if no subscription |
   | `plan_limits.max_projects`         | integer         | Maximum importable projects (KBs)                                        |
   | `plan_limits.max_output_projects`  | integer         | Maximum conversion output projects                                       |
   | `plan_limits.max_storage_gb`       | integer         | Maximum workspace storage in GB                                          |
   | `plan_limits.token_quota_5h`       | integer         | Token quota per 5-hour window                                            |
   | `plan_limits.token_quota_weekly`   | integer         | Token quota per weekly window                                            |

3. Update the version line at the top of the document from `0.1.0` to `0.2.0` and update the date to `2026-03-09`.
  </action>
  <verify>
Read docs/INTEGRATION-CONTRACT.md and confirm Section 4 includes plan_limits in both the JSON example and the field table. Verify the version is updated.
  </verify>
  <done>
INTEGRATION-CONTRACT.md Section 4 documents plan_limits object with all 5 fields, including nullable semantics. Version bumped to 0.2.0.
  </done>
</task>

</tasks>

<verification>
1. `cd /Users/gilberto/projetos/wxk/wxcode-adm/backend && /opt/homebrew/bin/python3.11 -m pytest tests/test_claude_provisioning.py -v` — all 16 tests pass
2. `grep -n "plan_limits" backend/src/wxcode_adm/tenants/router.py` — plan_limits appears in wxcode-config endpoint
3. `grep -n "plan_limits" docs/INTEGRATION-CONTRACT.md` — plan_limits documented in contract
4. Response shape matches: `{"tenant_id": ..., "database_name": ..., ..., "plan_limits": {"max_projects": 5, ...}}`
</verification>

<success_criteria>
- GET /tenants/{id}/wxcode-config returns plan_limits with 5 fields (max_projects, max_output_projects, max_storage_gb, token_quota_5h, token_quota_weekly) when tenant has a subscription
- GET /tenants/{id}/wxcode-config returns plan_limits=null when tenant has no subscription
- All 16 provisioning tests pass (14 existing + 2 new)
- INTEGRATION-CONTRACT.md documents the plan_limits object
- wxcode engine can now read tenant plan limits from wxcode-adm (FLOW-BREAK-01 closed)
</success_criteria>

<output>
After completion, create `.planning/phases/25-wxcode-config-plan-limits/25-01-SUMMARY.md`
</output>
