---
phase: 20-crypto-service-tenant-model-extension
plan: 02
type: execute
wave: 2
depends_on: ["20-01"]
files_modified:
  - backend/src/wxcode_adm/tenants/models.py
  - backend/alembic/versions/008_add_claude_wxcode_tenant_fields.py
  - backend/tests/test_tenant_model_extension.py
autonomous: true
requirements:
  - ENG-02
  - ENG-03
must_haves:
  truths:
    - "Tenant model has claude_oauth_token field that stores encrypted values"
    - "Tenant model has claude_default_model field defaulting to 'sonnet'"
    - "Tenant model has claude_max_concurrent_sessions field defaulting to 3"
    - "Tenant model has claude_monthly_token_budget field that is nullable (null = unlimited)"
    - "Tenant model has database_name field that is nullable"
    - "Tenant model has default_target_stack field defaulting to 'fastapi-jinja2'"
    - "Tenant model has neo4j_enabled field defaulting to True"
    - "Tenant model has status field defaulting to 'pending_setup'"
    - "Alembic migration 008 adds all 8 new columns to the tenants table"
    - "Existing tenant rows get correct defaults after migration"
    - "Tests confirm all new fields and defaults"
  artifacts:
    - path: "backend/src/wxcode_adm/tenants/models.py"
      provides: "Extended Tenant model with Claude and wxcode fields"
      contains: "claude_oauth_token"
    - path: "backend/alembic/versions/008_add_claude_wxcode_tenant_fields.py"
      provides: "Alembic migration adding 8 columns to tenants table"
      contains: "revision: str = \"008\""
    - path: "backend/tests/test_tenant_model_extension.py"
      provides: "Tests for new Tenant model fields"
      min_lines: 60
  key_links:
    - from: "backend/alembic/versions/008_add_claude_wxcode_tenant_fields.py"
      to: "backend/src/wxcode_adm/tenants/models.py"
      via: "migration columns match model fields"
      pattern: "add_column.*tenants"
    - from: "backend/tests/test_tenant_model_extension.py"
      to: "backend/src/wxcode_adm/tenants/models.py"
      via: "tests verify model field defaults and types"
      pattern: "Tenant\\("
---

<objective>
Extend the Tenant model with Claude and wxcode fields, create Alembic migration 008, and write tests.

Purpose: The wxcode engine needs per-tenant configuration (Claude token, model, session limits, database name, stack, status). These fields on the Tenant model are the foundation for all subsequent v3.0 phases.
Output: Extended Tenant model, migration 008, and passing field tests.
</objective>

<execution_context>
@/Users/gilberto/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gilberto/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-crypto-service-tenant-model-extension/20-01-SUMMARY.md
@backend/src/wxcode_adm/tenants/models.py
@backend/src/wxcode_adm/db/base.py
@backend/alembic/versions/007_add_super_admin_columns.py
@backend/tests/conftest.py
@backend/src/wxcode_adm/common/crypto.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend Tenant model with Claude and wxcode fields</name>
  <files>backend/src/wxcode_adm/tenants/models.py</files>
  <action>
Add 8 new fields to the `Tenant` class in `models.py`, after the existing `is_deleted` field and before the `memberships` relationship. Add a comment block "# Phase 20: Claude and wxcode integration fields (added by migration 008)".

New imports needed: `Integer` from sqlalchemy (String, Boolean already imported).

Fields to add:

1. `claude_oauth_token: Mapped[Optional[str]]` — `mapped_column(String(2048), nullable=True, default=None)` — stores the Fernet-encrypted Claude OAuth token. The value is encrypted/decrypted by `crypto.py` at the service layer (not at the model layer). String(2048) accommodates encrypted tokens which are longer than the original.

2. `claude_default_model: Mapped[str]` — `mapped_column(String(50), nullable=False, default="sonnet")` — the default Claude model for this tenant.

3. `claude_max_concurrent_sessions: Mapped[int]` — `mapped_column(Integer, nullable=False, default=3)` — max concurrent Claude sessions.

4. `claude_monthly_token_budget: Mapped[Optional[int]]` — `mapped_column(Integer, nullable=True, default=None)` — monthly token budget, null = unlimited.

5. `database_name: Mapped[Optional[str]]` — `mapped_column(String(100), nullable=True, default=None)` — wxcode MongoDB database name, pattern: "wxcode_t_{slug}".

6. `default_target_stack: Mapped[str]` — `mapped_column(String(50), nullable=False, default="fastapi-jinja2")` — default conversion target stack.

7. `neo4j_enabled: Mapped[bool]` — `mapped_column(Boolean, nullable=False, default=True)` — whether Neo4j graph is enabled for this tenant.

8. `status: Mapped[str]` — `mapped_column(String(20), nullable=False, default="pending_setup")` — tenant status: pending_setup, active, suspended, cancelled. Use native_enum=False approach (plain String) consistent with how MemberRole is stored (the project avoids PostgreSQL native enums per RESEARCH.md pitfall #1).

Do NOT use a Python enum for status — keep it as a plain String column with a default, consistent with the project's approach of avoiding native_enum issues. The valid values will be enforced at the service/API layer, not at the database level.
  </action>
  <verify>
  Run: `cd /Users/gilberto/projetos/wxk/wxcode-adm/backend && python3 -c "from wxcode_adm.tenants.models import Tenant; print([c.name for c in Tenant.__table__.columns if 'claude' in c.name or c.name in ('database_name','default_target_stack','neo4j_enabled','status')])"`
  Expected output: list of 8 new column names.
  </verify>
  <done>
  Tenant model has 8 new fields with correct types, defaults, and nullability. No existing fields modified.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Alembic migration 008 and write tests for new fields</name>
  <files>backend/alembic/versions/008_add_claude_wxcode_tenant_fields.py, backend/tests/test_tenant_model_extension.py</files>
  <action>
**Part A: Alembic migration 008**

Create `backend/alembic/versions/008_add_claude_wxcode_tenant_fields.py` following the exact pattern from migration 007:

```
revision: str = "008"
down_revision: Union[str, None] = "007"
```

Docstring: "Add Claude and wxcode fields to tenants table"

`upgrade()`:
- `op.add_column("tenants", sa.Column("claude_oauth_token", sa.String(2048), nullable=True))`
- `op.add_column("tenants", sa.Column("claude_default_model", sa.String(50), nullable=False, server_default=sa.text("'sonnet'")))`
- `op.add_column("tenants", sa.Column("claude_max_concurrent_sessions", sa.Integer(), nullable=False, server_default=sa.text("3")))`
- `op.add_column("tenants", sa.Column("claude_monthly_token_budget", sa.Integer(), nullable=True))`
- `op.add_column("tenants", sa.Column("database_name", sa.String(100), nullable=True))`
- `op.add_column("tenants", sa.Column("default_target_stack", sa.String(50), nullable=False, server_default=sa.text("'fastapi-jinja2'")))`
- `op.add_column("tenants", sa.Column("neo4j_enabled", sa.Boolean(), nullable=False, server_default=sa.text("true")))`
- `op.add_column("tenants", sa.Column("status", sa.String(20), nullable=False, server_default=sa.text("'pending_setup'")))`

Use `server_default` for all non-nullable columns so existing rows get correct defaults without data migration (same pattern as 007).

`downgrade()`:
- Drop all 8 columns in reverse order.

**Part B: Tests for new Tenant fields**

Create `backend/tests/test_tenant_model_extension.py` with these tests:

1. `test_tenant_claude_fields_defaults` — Create a Tenant with only name+slug (required fields), assert:
   - `claude_oauth_token is None`
   - `claude_default_model == "sonnet"`
   - `claude_max_concurrent_sessions == 3`
   - `claude_monthly_token_budget is None`

2. `test_tenant_wxcode_fields_defaults` — Create a Tenant with only name+slug, assert:
   - `database_name is None`
   - `default_target_stack == "fastapi-jinja2"`
   - `neo4j_enabled is True`
   - `status == "pending_setup"`

3. `test_tenant_claude_oauth_token_encrypted_roundtrip` — Create a Tenant, set `claude_oauth_token` to an encrypted value (using `encrypt_value` from crypto.py after monkeypatching settings), persist, reload, decrypt and assert original value. This tests the full flow: encrypt -> store -> load -> decrypt.

4. `test_tenant_status_values` — Create tenants with each valid status value ("pending_setup", "active", "suspended", "cancelled"), persist and reload, assert status matches.

5. `test_tenant_custom_claude_config` — Create a Tenant with custom values: `claude_default_model="opus"`, `claude_max_concurrent_sessions=10`, `claude_monthly_token_budget=1000000`. Persist, reload, assert values.

6. `test_tenant_database_name_pattern` — Create a Tenant with `database_name="wxcode_t_acme"`, persist, reload, assert value.

These are integration tests using the `client` fixture pattern from conftest.py. Since they only need DB access (not HTTP), use the `test_db` fixture directly:
```python
@pytest.mark.asyncio
async def test_tenant_xxx(test_db, monkeypatch):
    async with test_db() as session:
        tenant = Tenant(name="Test", slug="test")
        session.add(tenant)
        await session.flush()
        # assertions...
```

For the crypto round-trip test, monkeypatch `settings.WXCODE_ENCRYPTION_KEY` before importing/calling crypto functions.
  </action>
  <verify>
  Run: `cd /Users/gilberto/projetos/wxk/wxcode-adm/backend && python3 -m pytest tests/test_tenant_model_extension.py -v`
  Then run full test suite: `python3 -m pytest tests/ -v --tb=short` to confirm no regressions.
  </verify>
  <done>
  All 6 tenant model extension tests pass. Full test suite passes with no regressions. Migration 008 file exists with correct revision chain (007 -> 008).
  </done>
</task>

</tasks>

<verification>
- `python3 -m pytest tests/test_tenant_model_extension.py -v` — all 6 tests pass
- `python3 -m pytest tests/ -v --tb=short` — full suite passes (148+ existing tests + 6 new)
- `python3 -c "from wxcode_adm.tenants.models import Tenant; t = Tenant(name='X', slug='x'); print(t.status, t.claude_default_model, t.neo4j_enabled)"` — prints "pending_setup sonnet True"
- Migration 008 file exists with `down_revision = "007"` and all 8 `add_column` operations
</verification>

<success_criteria>
- Tenant model has all 8 new fields with correct types, defaults, and nullability
- Alembic migration 008 exists with down_revision="007" and adds all 8 columns with server_defaults
- 6 integration tests pass covering defaults, custom values, encrypted token round-trip, and status values
- No existing tests broken (full suite green)
- Zero breaking changes to existing endpoints
</success_criteria>

<output>
After completion, create `.planning/phases/20-crypto-service-tenant-model-extension/20-02-SUMMARY.md`
</output>
