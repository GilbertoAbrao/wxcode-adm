---
phase: 20-crypto-service-tenant-model-extension
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/wxcode_adm/common/crypto.py
  - backend/src/wxcode_adm/config.py
  - backend/tests/test_crypto.py
autonomous: true
requirements:
  - ENG-01
must_haves:
  truths:
    - "Fernet encrypt/decrypt round-trips any UTF-8 string correctly"
    - "Encrypted output is different from plaintext input"
    - "Decrypting with wrong key raises an error (not silent corruption)"
    - "WXCODE_ENCRYPTION_KEY is loaded from environment via Settings"
    - "Missing WXCODE_ENCRYPTION_KEY does not break existing app startup (has default for dev)"
  artifacts:
    - path: "backend/src/wxcode_adm/common/crypto.py"
      provides: "Fernet encrypt/decrypt service functions"
      exports: ["encrypt_value", "decrypt_value"]
    - path: "backend/src/wxcode_adm/config.py"
      provides: "WXCODE_ENCRYPTION_KEY setting"
      contains: "WXCODE_ENCRYPTION_KEY"
    - path: "backend/tests/test_crypto.py"
      provides: "Unit tests for crypto service"
      min_lines: 40
  key_links:
    - from: "backend/src/wxcode_adm/common/crypto.py"
      to: "backend/src/wxcode_adm/config.py"
      via: "imports settings.WXCODE_ENCRYPTION_KEY"
      pattern: "settings\\.WXCODE_ENCRYPTION_KEY"
---

<objective>
Create the Fernet encryption service and add the encryption key to application settings.

Purpose: Claude OAuth tokens stored in the Tenant model must be encrypted at rest. This plan provides the reusable encrypt/decrypt functions that Phase 20 Plan 02 (and later Phase 22) will use.
Output: `crypto.py` service module, updated `config.py`, and passing crypto tests.
</objective>

<execution_context>
@/Users/gilberto/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gilberto/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/src/wxcode_adm/config.py
@backend/src/wxcode_adm/common/__init__.py
@backend/tests/conftest.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Fernet crypto service and add encryption key to Settings</name>
  <files>backend/src/wxcode_adm/common/crypto.py, backend/src/wxcode_adm/config.py</files>
  <action>
1. Create `backend/src/wxcode_adm/common/crypto.py` with two public functions:
   - `encrypt_value(plaintext: str) -> str` — encrypts a UTF-8 string using Fernet, returns base64-encoded ciphertext string. Uses `settings.WXCODE_ENCRYPTION_KEY` from config.
   - `decrypt_value(ciphertext: str) -> str` — decrypts a Fernet ciphertext string back to UTF-8 plaintext. Raises `cryptography.fernet.InvalidToken` on wrong key or corrupted data.
   - Import `Fernet` from `cryptography.fernet`. Create the Fernet instance lazily or at function call time using `settings.WXCODE_ENCRYPTION_KEY.get_secret_value()`.
   - Do NOT create a module-level Fernet instance (settings may not be loaded yet at import time, and tests need to monkeypatch the key). Instead, create Fernet inside each function call, or use a helper `_get_fernet() -> Fernet` that reads from settings each time.

2. Add to `backend/src/wxcode_adm/config.py` in the Settings class:
   - `WXCODE_ENCRYPTION_KEY: SecretStr = SecretStr("dev-placeholder-key-do-not-use-in-prod")` — placed in a new "Phase 20" section after the Phase 8 section. The default is a dev placeholder so existing tests and dev environments don't break. In production, a real Fernet key (generated via `Fernet.generate_key()`) must be set.
   - Note: Fernet requires a 32-byte URL-safe base64 key. The `encrypt_value` function should handle the case where the key is the dev placeholder by still working (Fernet will accept any base64-padded key), but the placeholder will be obviously non-production.
   - Actually, Fernet is strict about key format (must be 32 bytes, URL-safe base64). Use `Fernet.generate_key().decode()` to generate a valid dev default. Hardcode a fixed valid Fernet key as the dev default, e.g.: `WXCODE_ENCRYPTION_KEY: SecretStr = SecretStr("dGVzdC1lbmNyeXB0aW9uLWtleS0xMjM0NTY3ODk=")` — but this won't work because Fernet keys must be exactly 44 chars of url-safe base64. Instead, generate one now: use `gAAAAAB...` format. The simplest approach: use a known valid Fernet key as default. Generate with: `from cryptography.fernet import Fernet; Fernet.generate_key()`. Use a fixed value like `"VGhpc0lzQVRlc3RLZXlGb3JEZXZPTMX5MTIzNDU2Nzg="` — no, just use a proper one. Set default to `SecretStr("RHVtbXlFbmNyeXB0aW9uS2V5Rm9yRGV2T25seSEhIQ==")` — this is "DummyEncryptionKeyForDevOnly!!!" base64'd, but Fernet needs url_safe_b64 of exactly 32 bytes. Simplest: just set a real generated key as the default dev value. Use `"dGhpcy1pcy1hLWRldi1vbmx5LWtleS0zMg=="` — actually, let's just be practical. Generate a valid key in the action and hardcode it. The key MUST be exactly 44 characters of URL-safe base64 (32 raw bytes). Use: `"ZEdocGN5MXBjeTFoTFdSbGRpMXZibXg1TFd0bGVTMHo="` which is valid base64 but may not decode to 32 bytes. SIMPLEST APPROACH: In crypto.py, if the key doesn't look like a valid Fernet key, generate a deterministic one from it using SHA256+base64. This way any string works as WXCODE_ENCRYPTION_KEY, and the dev default "change-me-in-production" just works.

   FINAL APPROACH (simple and correct):
   - In config.py: `WXCODE_ENCRYPTION_KEY: SecretStr = SecretStr("change-me-in-production")`
   - In crypto.py `_get_fernet()`: take the raw key string, if it's exactly 44 chars of url-safe base64 (valid Fernet key), use it directly. Otherwise, derive a valid Fernet key from it using `base64.urlsafe_b64encode(hashlib.sha256(key.encode()).digest())`. This way both dev (arbitrary string) and production (real Fernet key) work seamlessly.

3. No new pip dependency needed — `cryptography` is already installed via `PyJWT[crypto]`.
  </action>
  <verify>
  Run: `cd /Users/gilberto/projetos/wxk/wxcode-adm/backend && python3 -c "from wxcode_adm.common.crypto import encrypt_value, decrypt_value; ct = encrypt_value('hello'); assert decrypt_value(ct) == 'hello'; print('OK')"`
  </verify>
  <done>
  `encrypt_value` and `decrypt_value` functions exist in crypto.py, import cleanly, and round-trip correctly. `WXCODE_ENCRYPTION_KEY` exists in Settings with a dev default.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write tests for crypto service</name>
  <files>backend/tests/test_crypto.py</files>
  <action>
Create `backend/tests/test_crypto.py` with the following test cases (pytest, asyncio_mode=auto per project convention, but these are sync tests since crypto is synchronous):

1. `test_encrypt_decrypt_roundtrip` — encrypt a string, decrypt it, assert equality.
2. `test_encrypted_value_differs_from_plaintext` — encrypt "secret-token", assert ciphertext != "secret-token".
3. `test_encrypt_produces_different_ciphertexts` — encrypt same string twice, assert ciphertexts differ (Fernet uses random IV).
4. `test_decrypt_with_wrong_key_fails` — encrypt with one key, monkeypatch settings to a different key, attempt decrypt, assert raises `InvalidToken`.
5. `test_encrypt_empty_string` — encrypt empty string "", decrypt, assert "".
6. `test_encrypt_unicode` — encrypt a unicode string (e.g., "encriptacao segura 🔒"), decrypt, assert equality.

Use monkeypatch to set `settings.WXCODE_ENCRYPTION_KEY` to a known test key for deterministic behavior. Import `SecretStr` from pydantic for monkeypatching. Pattern:
```python
from wxcode_adm import config as config_module
monkeypatch.setattr(config_module.settings, "WXCODE_ENCRYPTION_KEY", SecretStr("test-key-1"))
```

For the wrong-key test, encrypt with "test-key-1", then monkeypatch to "test-key-2" before decrypting.
  </action>
  <verify>
  Run: `cd /Users/gilberto/projetos/wxk/wxcode-adm/backend && python3 -m pytest tests/test_crypto.py -v`
  </verify>
  <done>
  All 6 crypto tests pass. Coverage includes round-trip, uniqueness, wrong-key rejection, empty string, and unicode.
  </done>
</task>

</tasks>

<verification>
- `python3 -c "from wxcode_adm.common.crypto import encrypt_value, decrypt_value"` succeeds
- `python3 -c "from wxcode_adm.config import settings; print(settings.WXCODE_ENCRYPTION_KEY)"` shows the dev default
- `python3 -m pytest tests/test_crypto.py -v` — all tests pass
</verification>

<success_criteria>
- Fernet encrypt/decrypt service exists at `backend/src/wxcode_adm/common/crypto.py`
- `WXCODE_ENCRYPTION_KEY` setting exists in config.py with dev default
- 6 tests pass covering round-trip, ciphertext uniqueness, wrong-key rejection, edge cases
- No existing tests broken
</success_criteria>

<output>
After completion, create `.planning/phases/20-crypto-service-tenant-model-extension/20-01-SUMMARY.md`
</output>
