---
phase: 22-claude-provisioning-api
plan: 02
type: execute
wave: 2
depends_on: ["22-01"]
files_modified:
  - backend/src/wxcode_adm/tenants/router.py
  - backend/tests/test_claude_provisioning.py
autonomous: true
requirements:
  - GET-wxcode-config
  - tests

must_haves:
  truths:
    - "Tenant member with DEVELOPER+ role can retrieve wxcode config via GET /tenants/{id}/wxcode-config"
    - "wxcode-config response includes database_name, default_target_stack, neo4j_enabled, claude_default_model, max_concurrent_sessions"
    - "wxcode-config response does NOT include claude_oauth_token"
    - "PUT claude-token stores encrypted token, retrievable in admin detail"
    - "DELETE claude-token clears the token"
    - "PATCH claude-config updates partial config"
    - "POST activate transitions pending_setup to active"
    - "POST activate rejects non-pending_setup tenants"
    - "POST activate rejects tenants without database_name"
    - "All operations are audit-logged"
  artifacts:
    - path: "backend/src/wxcode_adm/tenants/router.py"
      provides: "GET /tenants/{id}/wxcode-config endpoint"
      contains: "wxcode-config"
    - path: "backend/tests/test_claude_provisioning.py"
      provides: "Integration tests for all 5 provisioning endpoints"
      min_lines: 200
  key_links:
    - from: "backend/src/wxcode_adm/tenants/router.py"
      to: "backend/src/wxcode_adm/tenants/dependencies.py"
      via: "require_role(MemberRole.DEVELOPER)"
      pattern: "require_role\\(MemberRole\\.DEVELOPER\\)"
    - from: "backend/tests/test_claude_provisioning.py"
      to: "backend/src/wxcode_adm/admin/router.py"
      via: "HTTP client calls to admin endpoints"
      pattern: "/api/v1/admin/tenants/"
    - from: "backend/tests/test_claude_provisioning.py"
      to: "backend/src/wxcode_adm/tenants/router.py"
      via: "HTTP client calls to wxcode-config"
      pattern: "/api/v1/tenants/.*/wxcode-config"
---

<objective>
Add the tenant-scoped GET /tenants/{id}/wxcode-config endpoint and comprehensive integration tests for all 5 Phase 22 endpoints.

Purpose: The wxcode-config endpoint lets the wxcode engine fetch tenant configuration (without the token). Tests validate all provisioning operations end-to-end including error cases and audit logging.

Output: 1 new tenant-scoped endpoint on tenants router + test file covering all 5 endpoints.
</objective>

<execution_context>
@/Users/gilberto/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gilberto/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/milestones/v3.0-ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-claude-provisioning-api/22-01-SUMMARY.md

@backend/src/wxcode_adm/tenants/router.py
@backend/src/wxcode_adm/tenants/dependencies.py
@backend/src/wxcode_adm/tenants/models.py
@backend/src/wxcode_adm/admin/router.py
@backend/src/wxcode_adm/admin/service.py
@backend/src/wxcode_adm/admin/schemas.py
@backend/tests/conftest.py
@backend/tests/test_super_admin.py
@backend/tests/test_tenant_model_extension.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add GET /tenants/{id}/wxcode-config endpoint</name>
  <files>backend/src/wxcode_adm/tenants/router.py</files>
  <action>
Add a new endpoint on the existing `router` (prefix="/tenants") in tenants/router.py.

This endpoint is tenant-scoped, NOT admin-scoped. It requires DEVELOPER+ membership and uses `require_role(MemberRole.DEVELOPER)` dependency.

Add under a new section "# Phase 22: wxcode engine config endpoint":

```python
@router.get(
    "/{tenant_id}/wxcode-config",
    summary="Get wxcode engine configuration for a tenant",
    description=(
        "Returns the wxcode engine configuration for the specified tenant. "
        "Includes database name, target stack, Neo4j flag, and Claude settings. "
        "Does NOT return the Claude OAuth token — it never leaves wxcode-adm. "
        "Requires DEVELOPER role or above."
    ),
)
async def get_wxcode_config(
    request: Request,
    tenant_id: uuid.UUID,
    ctx=Depends(require_role(MemberRole.DEVELOPER)),
    db: AsyncSession = Depends(get_session),
) -> dict:
```

Implementation:
- Extract `tenant, membership = ctx`
- Verify that `tenant.id == tenant_id` — if not, raise NotFoundError(error_code="TENANT_MISMATCH", message="Tenant ID does not match current tenant context"). This prevents a DEVELOPER in tenant A from reading config of tenant B by guessing UUIDs.
- Return dict with:
  ```python
  {
      "tenant_id": str(tenant.id),
      "database_name": tenant.database_name,
      "default_target_stack": tenant.default_target_stack,
      "neo4j_enabled": tenant.neo4j_enabled,
      "claude_default_model": tenant.claude_default_model,
      "max_concurrent_sessions": tenant.claude_max_concurrent_sessions,
  }
  ```
- Note: `claude_oauth_token` is explicitly NOT returned. The token never leaves wxcode-adm via any response.
- Note: `claude_monthly_token_budget` is NOT returned here (it is an admin-only operational limit, not needed by wxcode engine).
- `max_concurrent_sessions` field name (without `claude_` prefix) matches the ROADMAP deliverable spec.

The endpoint is on the tenants router which is already registered in main.py at `/api/v1`, so the full path will be `/api/v1/tenants/{tenant_id}/wxcode-config`.

Import uuid at the top of the file if not already imported (it is already imported).
Import NotFoundError from wxcode_adm.common.exceptions if not already imported (it is already imported).
  </action>
  <verify>
Run: `cd /Users/gilberto/projetos/wxk/wxcode-adm/backend && python -c "from wxcode_adm.tenants.router import router; routes = [r.path for r in router.routes]; assert '/{tenant_id}/wxcode-config' in routes; print('wxcode-config route registered')"`
  </verify>
  <done>GET /tenants/{tenant_id}/wxcode-config endpoint registered on tenants router. Requires DEVELOPER+ role. Returns config without Claude token. Validates tenant_id matches current context.</done>
</task>

<task type="auto">
  <name>Task 2: Add integration tests for all 5 provisioning endpoints</name>
  <files>backend/tests/test_claude_provisioning.py</files>
  <action>
Create `backend/tests/test_claude_provisioning.py` with comprehensive integration tests for all 5 Phase 22 endpoints.

Follow the pattern from `test_super_admin.py`:
- Use `client` fixture which yields `(http_client, fake_redis, app, test_db)`
- Use `_seed_super_admin` + `_admin_login` helpers (copy from test_super_admin.py or import — since they are local helpers, copy them)
- Use `_signup_verify_login` helper for regular user flow
- Use `monkeypatch` to patch WXCODE_ENCRYPTION_KEY for crypto tests

Helpers to include (copied from test_super_admin.py pattern):
- `_seed_super_admin(test_db, email, password)` — insert super-admin directly
- `_admin_login(client, email, password)` — login and return token dict
- `_signup_verify_login(client, redis, email, password)` — signup/verify/login regular user
- `_create_tenant_with_member(test_db, tenant_name, slug, user_email, user_password, role)` — helper that creates a tenant and a membership directly in DB for testing wxcode-config endpoint

Tests to write (all `@pytest.mark.asyncio`):

**Admin endpoints (use _seed_super_admin + _admin_login):**

1. `test_set_claude_token_success(client, test_db, monkeypatch)`:
   - Patch WXCODE_ENCRYPTION_KEY
   - Seed super-admin, login
   - Create a tenant in test_db
   - PUT /api/v1/admin/tenants/{id}/claude-token with `{"token": "test-oauth-token-abc", "reason": "Initial setup"}`
   - Assert 200, message contains "Claude token set"
   - GET /api/v1/admin/tenants/{id} — verify `has_claude_token` is True in response
   - Verify the raw token is NOT in the response

2. `test_set_claude_token_not_found(client, test_db, monkeypatch)`:
   - Seed super-admin, login
   - PUT with random UUID — assert 404

3. `test_revoke_claude_token_success(client, test_db, monkeypatch)`:
   - Patch WXCODE_ENCRYPTION_KEY
   - Seed super-admin, login, create tenant, set a token first
   - DELETE /api/v1/admin/tenants/{id}/claude-token with `{"reason": "Security rotation"}`
   - Assert 200
   - GET /api/v1/admin/tenants/{id} — verify `has_claude_token` is False

4. `test_revoke_claude_token_no_token(client, test_db, monkeypatch)`:
   - Create tenant without token
   - DELETE — assert 409 with error_code "NO_TOKEN"

5. `test_update_claude_config_partial(client, test_db, monkeypatch)`:
   - Seed super-admin, login, create tenant
   - PATCH /api/v1/admin/tenants/{id}/claude-config with `{"claude_default_model": "opus"}`
   - Assert 200
   - GET tenant detail — verify claude_default_model is "opus", other fields unchanged

6. `test_update_claude_config_all_fields(client, test_db, monkeypatch)`:
   - PATCH with all 3 fields: model="haiku", sessions=5, budget=500000
   - GET detail — verify all 3 updated

7. `test_update_claude_config_empty_rejected(client, test_db, monkeypatch)`:
   - PATCH with empty body `{}` — assert 422 (validation error from model_validator)

8. `test_activate_tenant_success(client, test_db, monkeypatch)`:
   - Seed super-admin, login
   - Create tenant with status="pending_setup" and database_name="wxcode_t_test"
   - POST /api/v1/admin/tenants/{id}/activate with `{"reason": "Setup complete"}`
   - Assert 200, message contains "activated"
   - GET detail — verify status is "active"

9. `test_activate_tenant_wrong_status(client, test_db, monkeypatch)`:
   - Create tenant with status="active"
   - POST activate — assert 409 with error_code "INVALID_STATUS"

10. `test_activate_tenant_no_database_name(client, test_db, monkeypatch)`:
    - Create tenant with status="pending_setup" but database_name=None
    - POST activate — assert 409 with error_code "MISSING_DATABASE_NAME"

**wxcode-config endpoint (use _signup_verify_login + tenant context):**

11. `test_wxcode_config_developer_access(client, test_db, monkeypatch)`:
    - Patch WXCODE_ENCRYPTION_KEY
    - Signup/verify/login a regular user
    - Create workspace via POST /api/v1/onboarding/workspace
    - Set database_name, default_target_stack, claude_default_model on the tenant directly in test_db
    - Set a claude_oauth_token (encrypted) directly in test_db
    - GET /api/v1/tenants/{tenant_id}/wxcode-config with X-Tenant-ID header
    - Assert 200
    - Assert response contains: database_name, default_target_stack, neo4j_enabled, claude_default_model, max_concurrent_sessions
    - Assert response does NOT contain: claude_oauth_token or any token field
    Note: The workspace creator is OWNER (level 4 >= DEVELOPER level 2), so they can access this endpoint.

12. `test_wxcode_config_viewer_denied(client, test_db, monkeypatch)`:
    - Create a user with VIEWER role in a tenant
    - GET wxcode-config — assert 403 (insufficient role)

13. `test_wxcode_config_tenant_mismatch(client, test_db, monkeypatch)`:
    - User is DEVELOPER in tenant A
    - GET /api/v1/tenants/{tenant_B_id}/wxcode-config with X-Tenant-ID of tenant A
    - Assert 404 (mismatch protection)

**Audit logging:**

14. `test_provisioning_audit_trail(client, test_db, monkeypatch)`:
    - Patch WXCODE_ENCRYPTION_KEY
    - Seed super-admin, login
    - Perform: set token, update config, activate
    - Query audit_logs table directly in test_db
    - Assert entries exist for: set_claude_token, update_claude_config, activate_tenant
    - Assert audit details do NOT contain the actual token value

For creating tenants in tests, insert directly into test_db:
```python
async with test_db() as session:
    tenant = Tenant(name="Test Corp", slug="test-corp", database_name="wxcode_t_test")
    session.add(tenant)
    await session.commit()
    tenant_id = tenant.id
```

For the wxcode-config tests that need a user with membership, use the workspace creation flow (POST /api/v1/onboarding/workspace) which creates tenant + OWNER membership automatically — then modify tenant fields directly in test_db.
  </action>
  <verify>
Run: `cd /Users/gilberto/projetos/wxk/wxcode-adm/backend && python -m pytest tests/test_claude_provisioning.py -v --tb=short`
  </verify>
  <done>All 14 tests pass. Coverage: PUT/DELETE claude-token (success + errors), PATCH claude-config (partial, full, empty), POST activate (success + wrong status + no database_name), GET wxcode-config (developer access, viewer denied, tenant mismatch), audit trail verification.</done>
</task>

</tasks>

<verification>
1. wxcode-config endpoint registered: `python -c "from wxcode_adm.tenants.router import router; print([r.path for r in router.routes])"`
2. All new tests pass: `cd backend && python -m pytest tests/test_claude_provisioning.py -v`
3. All existing tests still pass: `cd backend && python -m pytest tests/ -x -q`
4. Full endpoint inventory matches roadmap: PUT/DELETE claude-token, PATCH claude-config, POST activate, GET wxcode-config
</verification>

<success_criteria>
- GET /tenants/{id}/wxcode-config returns config without token, requires DEVELOPER+ role
- 14 integration tests cover all 5 endpoints including error cases
- All existing tests (157) continue passing
- Token value never appears in any response or audit log
- Audit entries exist for all provisioning operations
</success_criteria>

<output>
After completion, create `.planning/phases/22-claude-provisioning-api/22-02-SUMMARY.md`
</output>
