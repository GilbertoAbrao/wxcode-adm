---
phase: 02-auth-core
plan: 03
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
files_modified:
  - backend/src/wxcode_adm/auth/models.py
  - backend/src/wxcode_adm/auth/schemas.py
  - backend/src/wxcode_adm/auth/service.py
  - backend/src/wxcode_adm/auth/router.py
  - backend/src/wxcode_adm/auth/exceptions.py
autonomous: true
requirements:
  - AUTH-05
  - AUTH-06

must_haves:
  truths:
    - "POST /api/v1/auth/login with valid verified credentials returns access_token and refresh_token"
    - "Login rejects unverified users with 403 (EMAIL_NOT_VERIFIED)"
    - "Login rejects invalid credentials with 401 (same message for wrong email and wrong password)"
    - "New login revokes all previous refresh tokens for that user (single-session policy)"
    - "POST /api/v1/auth/refresh with valid refresh token returns new access_token and new refresh_token; old refresh token is invalidated"
    - "Replaying a consumed refresh token triggers full logout (all sessions revoked)"
    - "POST /api/v1/auth/logout invalidates the refresh token and blacklists the access token's jti in Redis"
    - "Blacklisted access tokens are rejected by get_current_user via is_token_blacklisted check on the token's jti claim (implemented in 02-05)"
  artifacts:
    - path: "backend/src/wxcode_adm/auth/models.py"
      provides: "RefreshToken SQLAlchemy model"
      contains: "class RefreshToken"
    - path: "backend/src/wxcode_adm/auth/schemas.py"
      provides: "Login, refresh, logout request/response schemas"
      contains: "class LoginRequest"
    - path: "backend/src/wxcode_adm/auth/service.py"
      provides: "Login, refresh, logout business logic with replay detection"
      contains: "async def login"
    - path: "backend/src/wxcode_adm/auth/router.py"
      provides: "Login, refresh, logout endpoints"
      contains: "/login"
    - path: "backend/src/wxcode_adm/auth/exceptions.py"
      provides: "ReplayDetectedError exception"
      contains: "class ReplayDetectedError"
  key_links:
    - from: "backend/src/wxcode_adm/auth/service.py"
      to: "backend/src/wxcode_adm/auth/models.py"
      via: "RefreshToken CRUD for rotation"
      pattern: "RefreshToken"
    - from: "backend/src/wxcode_adm/auth/service.py"
      to: "backend/src/wxcode_adm/auth/jwt.py"
      via: "create_access_token on login and refresh"
      pattern: "create_access_token"
    - from: "backend/src/wxcode_adm/auth/service.py"
      to: "backend/src/wxcode_adm/auth/password.py"
      via: "verify_password on login"
      pattern: "verify_password"
    - from: "backend/src/wxcode_adm/auth/service.py"
      to: "Redis"
      via: "Shadow keys for replay detection (auth:replay:{sha256})"
      pattern: "auth:replay:"
---

<objective>
Sign-in endpoint, refresh token rotation with DB storage, access token Redis blacklist on logout, and single-session enforcement.

Purpose: Complete the token lifecycle. Users can authenticate and receive JWT tokens, rotate refresh tokens securely, and log out with immediate invalidation. This enables all subsequent auth-protected features.

Output: Working POST /api/v1/auth/login, POST /api/v1/auth/refresh, POST /api/v1/auth/logout endpoints; RefreshToken model; access token blacklist via Redis.
</objective>

<execution_context>
@/Users/gilberto/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gilberto/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-auth-core/02-RESEARCH.md
@.planning/phases/02-auth-core/02-01-SUMMARY.md
@.planning/phases/02-auth-core/02-02-SUMMARY.md
@backend/src/wxcode_adm/auth/models.py
@backend/src/wxcode_adm/auth/schemas.py
@backend/src/wxcode_adm/auth/service.py
@backend/src/wxcode_adm/auth/router.py
@backend/src/wxcode_adm/auth/jwt.py
@backend/src/wxcode_adm/auth/password.py
@backend/src/wxcode_adm/auth/exceptions.py
@backend/src/wxcode_adm/config.py
@backend/src/wxcode_adm/dependencies.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: RefreshToken model, login/refresh/logout service logic, access token blacklist, and replay detection</name>
  <files>
    backend/src/wxcode_adm/auth/models.py
    backend/src/wxcode_adm/auth/schemas.py
    backend/src/wxcode_adm/auth/service.py
    backend/src/wxcode_adm/auth/exceptions.py
  </files>
  <action>
1. **auth/models.py** — Add RefreshToken model (append to existing file):
   - `class RefreshToken(TimestampMixin, Base)` — NOT TenantModel (platform-level auth)
   - `__tablename__ = "refresh_tokens"`
   - Columns:
     - `token: Mapped[str]` — `String(255)`, unique=True, index=True, nullable=False
     - `user_id: Mapped[uuid.UUID]` — `ForeignKey("users.id", ondelete="CASCADE")`, nullable=False, index=True
     - `expires_at: Mapped[datetime]` — `DateTime(timezone=True)`, nullable=False
   - Add import for ForeignKey if not present

2. **auth/schemas.py** — Add login, refresh, logout, and token response schemas:
   - `class LoginRequest(BaseModel)`: `email: EmailStr`, `password: str`
   - `class TokenResponse(BaseModel)`: `access_token: str`, `refresh_token: str`, `token_type: str = "bearer"`
   - `class RefreshRequest(BaseModel)`: `refresh_token: str`
   - `class LogoutRequest(BaseModel)`: `refresh_token: str` — also takes the access token from Authorization header in the endpoint

3. **auth/service.py** — Add login, refresh, logout functions, blacklist logic, and replay detection via Redis shadow keys:

   **Access token blacklist helpers:**
   - `async def blacklist_access_token(redis: Redis, token: str) -> None`:
     - Decode token to get jti and exp (use `jwt.decode` with `options={"verify_exp": False}` since we need to read exp even if expired)
     - Actually: use `decode_access_token` but catch TokenExpiredError and still extract jti — OR directly use `jwt.decode` with verify_exp=False here
     - Better approach: `payload = jwt.decode(token, settings.JWT_PUBLIC_KEY.get_secret_value(), algorithms=["RS256"], options={"verify_exp": False})`
     - Calculate remaining TTL: `remaining = int(payload["exp"] - datetime.now(timezone.utc).timestamp())`
     - If remaining > 0: `await redis.set(f"auth:blacklist:jti:{payload['jti']}", "1", ex=remaining)`

   - `async def is_token_blacklisted(redis: Redis, jti: str) -> bool`:
     - `return await redis.exists(f"auth:blacklist:jti:{jti}") > 0`

   **Replay detection shadow key helpers (LOCKED DECISION: replay detection triggers full logout):**

   Shadow keys map a consumed refresh token's SHA-256 hash to the user_id. When a consumed token is replayed and the DB row is already deleted, the shadow key allows recovering the user_id to perform full logout.

   - `_shadow_key(token: str) -> str`:
     - `return f"auth:replay:{hashlib.sha256(token.encode()).hexdigest()}"`
     - Import `hashlib` at module top

   - `async def _write_shadow_key(redis: Redis, token: str, user_id: str) -> None`:
     - Write shadow key with TTL equal to `settings.REFRESH_TOKEN_TTL_DAYS * 86400` (same as refresh token lifetime):
       `await redis.set(_shadow_key(token), user_id, ex=settings.REFRESH_TOKEN_TTL_DAYS * 86400)`

   - `async def _write_shadow_keys_bulk(redis: Redis, tokens: list[str], user_id: str) -> None`:
     - For each token string in the list, call `_write_shadow_key(redis, token, user_id)`.
     - Use a Redis pipeline for efficiency:
       ```python
       pipe = redis.pipeline()
       ttl = settings.REFRESH_TOKEN_TTL_DAYS * 86400
       for t in tokens:
           pipe.set(_shadow_key(t), user_id, ex=ttl)
       await pipe.execute()
       ```

   - `async def _check_replay_and_logout(db: AsyncSession, redis: Redis, token: str) -> None`:
     - Look up shadow key: `user_id = await redis.get(_shadow_key(token))`
     - If user_id is found (this IS a replay of a legitimately consumed token):
       - **Full logout:** Delete ALL RefreshToken rows for that user:
         `await db.execute(delete(RefreshToken).where(RefreshToken.user_id == uuid.UUID(user_id)))`
       - Raise `ReplayDetectedError()` (new exception — see below)
     - If shadow key not found (token was never valid, or shadow key expired):
       - Raise `InvalidTokenError()`

   - **Add `ReplayDetectedError`** to `auth/exceptions.py`:
     - Subclass of `AppError` (same as InvalidCredentialsError etc.)
     - Default status_code=401, error_code="REPLAY_DETECTED", message="Session compromised — all sessions revoked. Please log in again."

   **Login function:**
   - `async def login(db: AsyncSession, redis: Redis, body: LoginRequest) -> TokenResponse`:
     - Find user by email — if not found, raise `InvalidCredentialsError()`
     - If not user.is_active, raise `InvalidCredentialsError()` (same message)
     - Verify password: `verify_password(body.password, user.password_hash)` — if False, raise `InvalidCredentialsError()`
     - If not user.email_verified, raise `EmailNotVerifiedError(error_code="EMAIL_NOT_VERIFIED", message="Please verify your email before logging in")`
     - **Single-session enforcement:** Fetch existing refresh tokens for this user, write shadow keys for them, then delete:
       - `result = await db.execute(select(RefreshToken.token).where(RefreshToken.user_id == user.id))`
       - `old_tokens = [row[0] for row in result.all()]`
       - `if old_tokens: await _write_shadow_keys_bulk(redis, old_tokens, str(user.id))`
       - `await db.execute(delete(RefreshToken).where(RefreshToken.user_id == user.id))`
     - Create access token: `access_token = create_access_token(str(user.id))`
     - Create refresh token: `refresh_token_str = secrets.token_urlsafe(32)`
     - Store refresh token: `db.add(RefreshToken(token=refresh_token_str, user_id=user.id, expires_at=datetime.now(timezone.utc) + timedelta(days=settings.REFRESH_TOKEN_TTL_DAYS)))`
     - Return `TokenResponse` schema values (access_token, refresh_token_str)
     - Note: `db.flush()` is not strictly needed here because `get_session` commits after the endpoint returns

   **Refresh function:**
   - `async def refresh(db: AsyncSession, redis: Redis, body: RefreshRequest) -> TokenResponse`:
     - Find refresh token row: `select(RefreshToken).where(RefreshToken.token == body.refresh_token)`
     - If not found → **check replay shadow key and trigger full logout if applicable:**
       - `await _check_replay_and_logout(db, redis, body.refresh_token)`
       - (This either raises `ReplayDetectedError` if shadow key found, or `InvalidTokenError` if not)
     - If found but expired (`row.expires_at < datetime.now(timezone.utc)`): delete the row, raise `TokenExpiredError()`
     - **Rotation:** Write shadow key for old token, then delete old row and create new one:
       - `await _write_shadow_key(redis, row.token, str(row.user_id))`
       - `await db.delete(row)`
       - `new_token_str = secrets.token_urlsafe(32)`
       - `db.add(RefreshToken(token=new_token_str, user_id=row.user_id, expires_at=datetime.now(timezone.utc) + timedelta(days=settings.REFRESH_TOKEN_TTL_DAYS)))`
     - Create new access token: `access_token = create_access_token(str(row.user_id))`
     - Return TokenResponse values

   **Logout function:**
   - `async def logout(db: AsyncSession, redis: Redis, refresh_token: str, access_token: str) -> None`:
     - Find and delete the refresh token row: `select(RefreshToken).where(RefreshToken.token == refresh_token)` — delete if found (ignore if not found, idempotent)
     - Blacklist the access token: `await blacklist_access_token(redis, access_token)`
  </action>
  <verify>
    ```
    python -c "
    from wxcode_adm.auth.models import User, RefreshToken
    print('RefreshToken table:', RefreshToken.__tablename__)
    from wxcode_adm.auth.schemas import LoginRequest, TokenResponse, RefreshRequest, LogoutRequest
    print('Schemas OK')
    from wxcode_adm.auth.service import login, refresh, logout, blacklist_access_token, is_token_blacklisted
    print('Service functions OK')
    from wxcode_adm.auth.exceptions import ReplayDetectedError
    print('ReplayDetectedError OK')
    "
    ```
  </verify>
  <done>RefreshToken model defined, login/refresh/logout service functions implemented with single-session enforcement, access token blacklisting, and replay detection via Redis shadow keys triggering full logout (all sessions revoked).</done>
</task>

<task type="auto">
  <name>Task 2: Login, refresh, and logout router endpoints</name>
  <files>
    backend/src/wxcode_adm/auth/router.py
  </files>
  <action>
1. **auth/router.py** — Add login, refresh, and logout endpoints to `auth_api_router`:

   - `@auth_api_router.post("/login", response_model=TokenResponse)`
     - Params: `body: LoginRequest`, `db: AsyncSession = Depends(get_session)`, `redis: Redis = Depends(get_redis)`
     - Call `result = await service.login(db, redis, body)`
     - Return the TokenResponse (access_token, refresh_token, token_type="bearer")
     - Note: The service returns the data needed for TokenResponse. The router should construct the schema from the returned values.

   - `@auth_api_router.post("/refresh", response_model=TokenResponse)`
     - Params: `body: RefreshRequest`, `db: AsyncSession = Depends(get_session)`, `redis: Redis = Depends(get_redis)`
     - Call `result = await service.refresh(db, redis, body)`
     - Return TokenResponse

   - `@auth_api_router.post("/logout", response_model=MessageResponse)`
     - Params: `body: LogoutRequest`, `db: AsyncSession = Depends(get_session)`, `redis: Redis = Depends(get_redis)`, plus extract access token from Authorization header
     - To extract the access token: use `authorization: str | None = Header(default=None)` and parse it:
       ```python
       access_token = ""
       if authorization and authorization.startswith("Bearer "):
           access_token = authorization[7:]
       ```
     - Call `await service.logout(db, redis, body.refresh_token, access_token)`
     - Return `MessageResponse(message="Logged out successfully")`

   - Import `Header` from fastapi for the logout endpoint
   - Import all necessary schemas: `TokenResponse`, `LoginRequest`, `RefreshRequest`, `LogoutRequest`, `MessageResponse`
  </action>
  <verify>
    ```
    python -c "
    from wxcode_adm.auth.router import auth_api_router
    routes = [r.path for r in auth_api_router.routes]
    assert '/login' in routes, f'Missing /login: {routes}'
    assert '/refresh' in routes, f'Missing /refresh: {routes}'
    assert '/logout' in routes, f'Missing /logout: {routes}'
    print('Auth API routes:', routes)
    "
    ```
  </verify>
  <done>Login, refresh, and logout endpoints registered. Login returns access+refresh tokens. Refresh performs token rotation. Logout blacklists access token and deletes refresh token.</done>
</task>

</tasks>

<verification>
1. RefreshToken model inherits from TimestampMixin+Base (NOT TenantModel)
2. Login rejects unverified users, wrong passwords, and inactive users
3. Login deletes all existing refresh tokens before creating new one (single-session), writing shadow keys for each deleted token
4. Refresh consumes old token, writes shadow key for consumed token, and issues new pair
5. Replaying a consumed refresh token checks shadow key, finds user_id, deletes ALL that user's refresh tokens (full logout), and raises ReplayDetectedError
6. Logout deletes refresh token row and blacklists access token jti in Redis
7. All three endpoints registered at correct paths
</verification>

<success_criteria>
- POST /api/v1/auth/login returns access_token + refresh_token for verified users
- POST /api/v1/auth/refresh rotates tokens with immediate old-token invalidation
- POST /api/v1/auth/logout invalidates both tokens
- Single-session policy enforced: new login kills previous sessions
- Access token blacklist checked via Redis jti lookup
</success_criteria>

<output>
After completion, create `.planning/phases/02-auth-core/02-03-SUMMARY.md`
</output>
