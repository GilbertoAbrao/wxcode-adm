---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - backend/src/wxcode_adm/common/redis_client.py
  - backend/src/wxcode_adm/dependencies.py
  - backend/src/wxcode_adm/main.py
  - backend/src/wxcode_adm/common/router.py
  - backend/src/wxcode_adm/tasks/worker.py
autonomous: true
requirements:
  - "Enabler for all v1 requirements"

must_haves:
  truths:
    - "FastAPI app starts with lifespan that verifies PostgreSQL and Redis connectivity"
    - "GET /api/v1/health returns 200 with postgresql=ok and redis=ok when both services are up"
    - "GET /api/v1/health returns 503 with error details when PostgreSQL or Redis is down"
    - "arq worker starts as separate process and processes test_job successfully"
    - "get_session dependency yields AsyncSession with commit-on-success and rollback-on-error"
  artifacts:
    - path: "backend/src/wxcode_adm/main.py"
      provides: "FastAPI app factory with lifespan"
      contains: "create_app"
    - path: "backend/src/wxcode_adm/common/redis_client.py"
      provides: "Redis async client singleton"
      contains: "redis_client"
    - path: "backend/src/wxcode_adm/common/router.py"
      provides: "Health endpoint"
      contains: "/health"
    - path: "backend/src/wxcode_adm/dependencies.py"
      provides: "Session and Redis FastAPI dependencies"
      contains: "get_session"
    - path: "backend/src/wxcode_adm/tasks/worker.py"
      provides: "arq WorkerSettings with test_job"
      contains: "class WorkerSettings"
  key_links:
    - from: "backend/src/wxcode_adm/main.py"
      to: "backend/src/wxcode_adm/db/engine.py"
      via: "lifespan verifies PostgreSQL with SELECT 1"
      pattern: "engine\\.begin.*SELECT 1"
    - from: "backend/src/wxcode_adm/main.py"
      to: "backend/src/wxcode_adm/common/redis_client.py"
      via: "lifespan verifies Redis with ping()"
      pattern: "redis.*ping"
    - from: "backend/src/wxcode_adm/main.py"
      to: "backend/src/wxcode_adm/db/tenant.py"
      via: "lifespan installs tenant guard"
      pattern: "install_tenant_guard"
    - from: "backend/src/wxcode_adm/common/router.py"
      to: "backend/src/wxcode_adm/dependencies.py"
      via: "health endpoint uses get_session and get_redis"
      pattern: "Depends\\(get_session\\)"
    - from: "backend/src/wxcode_adm/tasks/worker.py"
      to: "backend/src/wxcode_adm/config.py"
      via: "RedisSettings reads from settings.REDIS_URL"
      pattern: "settings\\.REDIS_URL"
---

<objective>
Build the FastAPI application layer: app factory with lifespan (verifying PostgreSQL + Redis on startup), session dependency, Redis client, health endpoint returning live infrastructure status, and arq worker with a test job.

Purpose: This makes the application runnable. After this plan, `uvicorn wxcode_adm.main:app` starts a working server and `arq wxcode_adm.tasks.worker.WorkerSettings` starts a working job processor.
Output: Running FastAPI app with health endpoint and operational arq worker.
</objective>

<execution_context>
@/Users/gilberto/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gilberto/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redis client, session dependency, FastAPI app factory with lifespan</name>
  <files>
    backend/src/wxcode_adm/common/redis_client.py
    backend/src/wxcode_adm/dependencies.py
    backend/src/wxcode_adm/main.py
  </files>
  <action>
    **backend/src/wxcode_adm/common/redis_client.py:**
    - Create `redis_client` as module-level singleton: `redis_client = Redis.from_url(settings.REDIS_URL, decode_responses=True)`
    - Import `from redis.asyncio import Redis` (NOT the deprecated `aioredis` package)
    - Create `async def get_redis() -> Redis` dependency that returns `redis_client` (for FastAPI Depends injection)

    **backend/src/wxcode_adm/dependencies.py:**
    - `async def get_session() -> AsyncGenerator[AsyncSession, None]`: yields session from async_session_maker, commits on success, rolls back on exception (try/yield/commit/except rollback/raise pattern)
    - Import async_session_maker from wxcode_adm.db.engine
    - Import AsyncSession from sqlalchemy.ext.asyncio
    - Use `from collections.abc import AsyncGenerator` for type hint

    **backend/src/wxcode_adm/main.py:**
    - `@asynccontextmanager async def lifespan(app: FastAPI)`:
      1. Verify PostgreSQL: `async with engine.begin() as conn: await conn.execute(text("SELECT 1"))`
      2. Install tenant guard: `install_tenant_guard(async_session_maker)`
      3. Verify Redis: `await redis_client.ping()`
      4. Comment stub for super-admin seed (Phase 2)
      5. `yield`
      6. Shutdown: `await engine.dispose()`, `await redis_client.aclose()`
    - `def create_app() -> FastAPI`:
      - FastAPI instance with title="wxcode-adm", version="0.1.0"
      - openapi_url=f"{settings.API_V1_PREFIX}/openapi.json"
      - docs_url=f"{settings.API_V1_PREFIX}/docs"
      - lifespan=lifespan
      - Register common_router with prefix=settings.API_V1_PREFIX
      - Add CORS middleware with settings.ALLOWED_ORIGINS, allow_credentials=True, allow_methods=["*"], allow_headers=["*"]
      - Add exception handler for AppError that converts to JSONResponse with error_code and message
    - Module-level: `app = create_app()`
    - IMPORTANT: Do NOT use `@app.on_event("startup")` — it is deprecated. Use lifespan only.
    - IMPORTANT: Do NOT start arq worker inside lifespan — it must be a separate process.
  </action>
  <verify>
    Start PostgreSQL and Redis locally (or via docker).
    Create a .env file from .env.example with valid dummy JWT keys (generate with: `python -c "from cryptography.hazmat.primitives.asymmetric import rsa; from cryptography.hazmat.primitives import serialization; k=rsa.generate_private_key(65537,2048); print(k.private_bytes(serialization.Encoding.PEM,serialization.PrivateFormat.PKCS8,serialization.NoEncryption()).decode()); print(k.public_key().public_bytes(serialization.Encoding.PEM,serialization.PublicFormat.SubjectPublicKeyInfo).decode())"`).
    Run `cd /Users/gilberto/projetos/wxk/wxcode-adm/backend && python -m uvicorn wxcode_adm.main:app --host 0.0.0.0 --port 8060` — app must start without errors.
    Verify startup logs show PostgreSQL and Redis connectivity checks passing.
  </verify>
  <done>FastAPI app starts with lifespan verifying PostgreSQL and Redis. Tenant guard installed on session factory. Session dependency yields async sessions with proper commit/rollback. Redis client is a singleton from redis.asyncio. CORS middleware configured. AppError exception handler registered.</done>
</task>

<task type="auto">
  <name>Task 2: Health endpoint and arq worker with test job</name>
  <files>
    backend/src/wxcode_adm/common/router.py
    backend/src/wxcode_adm/tasks/worker.py
  </files>
  <action>
    **backend/src/wxcode_adm/common/router.py:**
    - Create `router = APIRouter(tags=["common"])`
    - `GET /health` endpoint:
      - Dependencies: `db: AsyncSession = Depends(get_session)`, `redis: Redis = Depends(get_redis)`
      - Check PostgreSQL: `await db.execute(text("SELECT 1"))` in try/except
      - Check Redis: `await redis.ping()` in try/except
      - Build checks dict: {"postgresql": "ok" or "error: {e}", "redis": "ok" or "error: {e}"}
      - If any check fails, raise HTTPException(status_code=503, detail=checks)
      - Return {"status": "healthy", "checks": checks}

    **backend/src/wxcode_adm/tasks/worker.py:**
    - `async def test_job(ctx: dict) -> str`: returns "arq worker is operational"
    - `async def startup(ctx: dict) -> None`: verify DB connection with `async with engine.begin() as conn: await conn.execute(text("SELECT 1"))`, store session_maker in ctx
    - `async def shutdown(ctx: dict) -> None`: `await engine.dispose()`
    - `class WorkerSettings`: functions=[test_job], on_startup=startup, on_shutdown=shutdown, redis_settings=RedisSettings.from_dsn(settings.REDIS_URL), max_jobs=10, job_timeout=300
    - Helper `async def get_arq_pool()`: returns `await create_pool(RedisSettings.from_dsn(settings.REDIS_URL))` — used for enqueueing jobs from API code

    NOTE on `execute_sql`: Research code example uses `conn.execute_sql("SELECT 1")` in arq startup. This is WRONG for SQLAlchemy — use `conn.execute(text("SELECT 1"))` with `from sqlalchemy import text`. The research had a typo.
  </action>
  <verify>
    With the app running: `curl http://localhost:8060/api/v1/health` — must return 200 with {"status": "healthy", "checks": {"postgresql": "ok", "redis": "ok"}}.
    Start the arq worker: `cd /Users/gilberto/projetos/wxk/wxcode-adm/backend && arq wxcode_adm.tasks.worker.WorkerSettings` — must start without errors.
    Enqueue a test job: `python -c "import asyncio; from wxcode_adm.tasks.worker import get_arq_pool; async def t(): p=await get_arq_pool(); j=await p.enqueue_job('test_job'); print(f'Job {j.job_id} enqueued'); await p.aclose(); asyncio.run(t())"` — job should be processed by the worker.
  </verify>
  <done>Health endpoint returns 200 with live PostgreSQL and Redis checks (or 503 on failure). arq worker starts as separate process, connects to Redis, verifies PostgreSQL, and processes test_job successfully. get_arq_pool helper available for enqueueing from API code.</done>
</task>

</tasks>

<verification>
1. `curl http://localhost:8060/api/v1/health` returns 200 with postgresql=ok and redis=ok
2. Stop PostgreSQL, `curl /health` returns 503 with postgresql error
3. `arq wxcode_adm.tasks.worker.WorkerSettings` starts and processes test_job
4. FastAPI docs accessible at http://localhost:8060/api/v1/docs
5. CORS headers present in response for allowed origins
</verification>

<success_criteria>
- FastAPI app starts successfully with lifespan infrastructure verification
- Health endpoint returns live status of PostgreSQL and Redis
- arq worker runs as separate process and processes jobs
- Session dependency properly manages commit/rollback lifecycle
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
