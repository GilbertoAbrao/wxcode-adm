---
phase: 01-foundation
plan: "04"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/wxcode_adm/db/tenant.py
  - backend/tests/test_tenant_guard.py
autonomous: true
requirements: []
gap_closure: true

must_haves:
  truths:
    - "An ORM SELECT on a TenantModel subclass without _tenant_enforced execution option raises TenantIsolationError"
    - "Health checks and raw SQL queries are unaffected by the tenant guard"
    - "A test proves the guard raises on unguarded queries and passes on guarded queries"
  artifacts:
    - path: "backend/src/wxcode_adm/db/tenant.py"
      provides: "Tenant guard that raises TenantIsolationError on unguarded queries"
      contains: "raise TenantIsolationError"
    - path: "backend/tests/test_tenant_guard.py"
      provides: "Test coverage for tenant guard enforcement"
      contains: "TenantIsolationError"
  key_links:
    - from: "backend/src/wxcode_adm/db/tenant.py"
      to: "backend/src/wxcode_adm/common/exceptions.py"
      via: "imports TenantIsolationError"
      pattern: "from wxcode_adm\\.common\\.exceptions import TenantIsolationError"
---

<objective>
Close ROADMAP SC#2 gap: upgrade the tenant isolation guard from advisory WARNING to enforcing RuntimeError.

Purpose: ROADMAP SC#2 states "a query without tenant_id is a runtime error, not a silent bug." The current implementation logs a warning and continues. Since no TenantModel subclasses exist yet and health checks use raw SQL (`SELECT 1`), upgrading to `raise TenantIsolationError(...)` has zero breakage risk. This closes the only gap found during Phase 1 verification.

Output: Updated tenant.py with hard raise, plus a test proving enforcement works.
</objective>

<execution_context>
@/Users/gilberto/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gilberto/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-VERIFICATION.md
@backend/src/wxcode_adm/db/tenant.py
@backend/src/wxcode_adm/common/exceptions.py
@backend/src/wxcode_adm/db/base.py
@backend/src/wxcode_adm/db/engine.py
@backend/pyproject.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade tenant guard from WARNING to TenantIsolationError raise</name>
  <files>backend/src/wxcode_adm/db/tenant.py</files>
  <action>
In `backend/src/wxcode_adm/db/tenant.py`:

1. Add import at the top: `from wxcode_adm.common.exceptions import TenantIsolationError`

2. In the `_enforce_tenant_id` inner function (inside `install_tenant_guard`), replace the `logger.warning(...)` call (around line 84) with:
```python
raise TenantIsolationError(
    f"Unguarded query on TenantModel subclass '{mapper.class_.__name__}'. "
    "All queries on tenant-scoped models must include tenant_id context. "
    "Pass execution_options(_tenant_enforced=True) or use the TenantSession helper."
)
```

3. Update the TenantModel class docstring to remove references to "logs a WARNING in Phase 1" and "upgraded to a hard RuntimeError raise in Phase 3". Replace with: "Any ORM SELECT on a TenantModel subclass that does NOT set the `_tenant_enforced` execution option raises `TenantIsolationError`."

4. Update the `install_tenant_guard` function docstring to remove "Phase 1: Logs WARNING" / "Phase 3: Upgrade to RuntimeError" lines. Replace with: "Raises TenantIsolationError on unguarded queries against TenantModel subclasses."

5. The `logger` import can remain (it may be useful for future debug logging), but remove the `logger.warning` call entirely — do not keep it as a fallback.
  </action>
  <verify>
Run: `cd /Users/gilberto/projetos/wxk/wxcode-adm/backend && python -c "from wxcode_adm.db.tenant import install_tenant_guard; print('import ok')"`
Confirm no import errors. Then grep for the old pattern:
`grep -n "logger.warning" backend/src/wxcode_adm/db/tenant.py` — should return nothing.
`grep -n "TenantIsolationError" backend/src/wxcode_adm/db/tenant.py` — should show the raise statement.
  </verify>
  <done>
The tenant guard raises TenantIsolationError instead of logging a warning. Docstrings reflect the enforcing behavior. No references to "Phase 1 warning" or "Phase 3 upgrade" remain in tenant.py.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add test proving tenant guard raises on unguarded queries</name>
  <files>backend/tests/__init__.py, backend/tests/test_tenant_guard.py</files>
  <action>
Create `backend/tests/__init__.py` (empty file — makes tests a package).

Create `backend/tests/test_tenant_guard.py` with:

1. A concrete TenantModel subclass defined inline for testing:
```python
from wxcode_adm.db.tenant import TenantModel, install_tenant_guard
from wxcode_adm.db.base import Base
from wxcode_adm.common.exceptions import TenantIsolationError
from sqlalchemy.ext.asyncio import create_async_engine, async_sessionmaker, AsyncSession
from sqlalchemy import select
import pytest
import uuid

class FakeTenantItem(TenantModel):
    __tablename__ = "fake_tenant_item"
    name: Mapped[str] = mapped_column(default="test")
```
(Import `Mapped, mapped_column` from `sqlalchemy.orm`)

2. A pytest fixture `guarded_session` that:
   - Creates an in-memory SQLite async engine: `create_async_engine("sqlite+aiosqlite://", echo=False)`
   - Creates all tables: `async with engine.begin() as conn: await conn.run_sync(Base.metadata.create_all)`
   - Creates `async_sessionmaker(engine, class_=AsyncSession, expire_on_commit=False)`
   - Calls `install_tenant_guard(session_maker)`
   - Yields the session_maker
   - Drops all tables in teardown

3. Test `test_unguarded_query_raises_tenant_isolation_error`:
   - Open a session from the fixture
   - Execute `select(FakeTenantItem)` WITHOUT `execution_options(_tenant_enforced=True)`
   - Assert that `TenantIsolationError` is raised
   - Use `pytest.raises(TenantIsolationError)`

4. Test `test_guarded_query_passes`:
   - Open a session from the fixture
   - Execute `select(FakeTenantItem).execution_options(_tenant_enforced=True)`
   - Assert NO exception is raised (result is empty list, which is fine)

5. Test `test_raw_sql_not_affected`:
   - Open a session from the fixture
   - Execute `text("SELECT 1")` — raw SQL should NOT trigger the guard
   - Assert no exception

Add `aiosqlite` to dev dependencies in `backend/pyproject.toml` (append to the `dev` list):
```
"aiosqlite",
```

Run `pip install -e ".[dev]"` to install aiosqlite.
  </action>
  <verify>
Run: `cd /Users/gilberto/projetos/wxk/wxcode-adm/backend && pip install -e ".[dev]" && python -m pytest tests/test_tenant_guard.py -v`
All 3 tests must pass.
  </verify>
  <done>
Three tests pass: unguarded query raises TenantIsolationError, guarded query succeeds, raw SQL is unaffected. Test infrastructure (tests/ directory, aiosqlite dev dependency) is in place for future phases.
  </done>
</task>

</tasks>

<verification>
1. `grep -n "raise TenantIsolationError" backend/src/wxcode_adm/db/tenant.py` — confirms hard enforcement
2. `grep -n "logger.warning" backend/src/wxcode_adm/db/tenant.py` — should return nothing (no advisory mode)
3. `cd backend && python -m pytest tests/test_tenant_guard.py -v` — all 3 tests pass
4. `cd backend && python -c "from wxcode_adm.db.tenant import install_tenant_guard"` — no import errors
5. Health endpoint is unaffected because it uses `text("SELECT 1")`, not ORM queries on TenantModel subclasses
</verification>

<success_criteria>
- ROADMAP SC#2 gap is closed: unguarded TenantModel queries raise TenantIsolationError (not a warning)
- Three passing tests prove the guard works correctly
- Health checks and raw SQL remain unaffected
- No regressions — existing code continues to function
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-04-SUMMARY.md`
</output>
