---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/pyproject.toml
  - backend/src/wxcode_adm/__init__.py
  - backend/src/wxcode_adm/config.py
  - backend/src/wxcode_adm/db/__init__.py
  - backend/src/wxcode_adm/db/engine.py
  - backend/src/wxcode_adm/db/base.py
  - backend/src/wxcode_adm/db/tenant.py
  - backend/src/wxcode_adm/common/__init__.py
  - backend/src/wxcode_adm/common/exceptions.py
  - backend/src/wxcode_adm/tasks/__init__.py
  - backend/src/wxcode_adm/auth/__init__.py
  - backend/src/wxcode_adm/tenants/__init__.py
  - backend/src/wxcode_adm/billing/__init__.py
  - backend/src/wxcode_adm/users/__init__.py
  - backend/alembic.ini
  - backend/alembic/env.py
  - backend/alembic/script.py.mako
  - backend/alembic/versions/.gitkeep
  - backend/.env.example
  - frontend/.gitkeep
autonomous: true
requirements:
  - "Enabler for all v1 requirements"

must_haves:
  truths:
    - "pyproject.toml declares all Phase 1 dependencies and the project is installable with pip install -e ."
    - "pydantic-settings loads config from .env and raises ValidationError on missing required fields at import time"
    - "SQLAlchemy async engine connects to PostgreSQL via asyncpg with expire_on_commit=False"
    - "TenantModel base class includes tenant_id column and do_orm_execute guard scaffold (logged warning)"
    - "Alembic is initialized with async template and env.py reads DATABASE_URL from pydantic-settings"
    - "Domain module directories exist (auth/, tenants/, billing/, users/) with __init__.py"
  artifacts:
    - path: "backend/pyproject.toml"
      provides: "Project metadata, dependencies, build config"
      contains: "wxcode-adm"
    - path: "backend/src/wxcode_adm/config.py"
      provides: "pydantic-settings Settings singleton"
      contains: "class Settings"
    - path: "backend/src/wxcode_adm/db/engine.py"
      provides: "Async engine and session factory"
      contains: "async_session_maker"
    - path: "backend/src/wxcode_adm/db/base.py"
      provides: "DeclarativeBase with naming conventions and TimestampMixin"
      contains: "class Base"
    - path: "backend/src/wxcode_adm/db/tenant.py"
      provides: "TenantModel base class with tenant_id and guard"
      contains: "class TenantModel"
    - path: "backend/alembic/env.py"
      provides: "Async Alembic migration runner"
      contains: "run_async_migrations"
  key_links:
    - from: "backend/src/wxcode_adm/db/engine.py"
      to: "backend/src/wxcode_adm/config.py"
      via: "settings.DATABASE_URL"
      pattern: "settings\\.DATABASE_URL"
    - from: "backend/src/wxcode_adm/db/tenant.py"
      to: "backend/src/wxcode_adm/db/base.py"
      via: "inherits Base and TimestampMixin"
      pattern: "class TenantModel.*Base"
    - from: "backend/alembic/env.py"
      to: "backend/src/wxcode_adm/config.py"
      via: "settings.DATABASE_URL override"
      pattern: "settings\\.DATABASE_URL"
---

<objective>
Create the monorepo skeleton, install all Phase 1 Python dependencies, configure pydantic-settings with startup validation, set up SQLAlchemy 2.0 async engine with TenantModel base class and tenant isolation guard, initialize Alembic with async template, and establish the domain-based module structure.

Purpose: Every subsequent phase builds on this foundation. The database layer, config system, and project structure must exist and be correct before any application code.
Output: Installable Python package with config, database engine, TenantModel, Alembic, and domain module scaffolding.
</objective>

<execution_context>
@/Users/gilberto/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gilberto/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create monorepo structure and Python package with all dependencies</name>
  <files>
    backend/pyproject.toml
    backend/src/wxcode_adm/__init__.py
    backend/src/wxcode_adm/auth/__init__.py
    backend/src/wxcode_adm/tenants/__init__.py
    backend/src/wxcode_adm/billing/__init__.py
    backend/src/wxcode_adm/users/__init__.py
    backend/src/wxcode_adm/common/__init__.py
    backend/src/wxcode_adm/db/__init__.py
    backend/src/wxcode_adm/tasks/__init__.py
    backend/.env.example
    frontend/.gitkeep
  </files>
  <action>
    Create the monorepo directory structure per user decision: `/backend` and `/frontend` at repo root.

    **backend/pyproject.toml:**
    - name: "wxcode-adm", version: "0.1.0", requires-python: ">=3.11"
    - Dependencies (pinned versions from research):
      fastapi==0.131.0, uvicorn[standard]==0.41.0, sqlalchemy==2.0.46, asyncpg==0.31.0,
      alembic==1.18.4, pydantic-settings==2.13.1, redis==7.2.0, arq==0.27.0
    - Dev dependencies: pytest, pytest-asyncio, httpx
    - [tool.pytest.ini_options] asyncio_mode = "auto"
    - Build system: hatchling with packages = ["src/wxcode_adm"]

    **backend/src/wxcode_adm/__init__.py:** Empty (package marker).

    **Domain module __init__.py files:** Create empty __init__.py in auth/, tenants/, billing/, users/, common/, db/, tasks/. These are placeholder directories for future phases.

    **backend/.env.example:** Template with all required env vars (with placeholder values):
    - APP_ENV=development, DEBUG=true, BACKEND_PORT=8060
    - DATABASE_URL=postgresql+asyncpg://wxcode:wxcode_dev@localhost:5432/wxcode_adm
    - REDIS_URL=redis://localhost:6379/0
    - JWT_PRIVATE_KEY="<RSA private key PEM>" (placeholder — Phase 2)
    - JWT_PUBLIC_KEY="<RSA public key PEM>" (placeholder — Phase 2)
    - SUPER_ADMIN_EMAIL=admin@wxcode.io
    - SUPER_ADMIN_PASSWORD=changeme
    - ALLOWED_ORIGINS=["http://localhost:3060"]

    **frontend/.gitkeep:** Empty file to track the frontend/ directory in git (Phase 7+).

    After creating files, run: `cd backend && pip install -e ".[dev]"` to verify the package installs correctly.
  </action>
  <verify>
    Run `cd /Users/gilberto/projetos/wxk/wxcode-adm/backend && pip install -e ".[dev]"` — must succeed with no errors.
    Run `python -c "import wxcode_adm"` — must not raise ImportError.
    Verify directory structure: `ls backend/src/wxcode_adm/` shows auth/, tenants/, billing/, users/, common/, db/, tasks/.
  </verify>
  <done>pyproject.toml exists with all Phase 1 dependencies pinned. Package installs with pip install -e . All domain module directories exist with __init__.py. .env.example documents all required environment variables.</done>
</task>

<task type="auto">
  <name>Task 2: Config, database layer, TenantModel, Alembic, and shared exceptions</name>
  <files>
    backend/src/wxcode_adm/config.py
    backend/src/wxcode_adm/db/engine.py
    backend/src/wxcode_adm/db/base.py
    backend/src/wxcode_adm/db/tenant.py
    backend/src/wxcode_adm/common/exceptions.py
    backend/alembic.ini
    backend/alembic/env.py
    backend/alembic/script.py.mako
    backend/alembic/versions/.gitkeep
  </files>
  <action>
    **backend/src/wxcode_adm/config.py:**
    - Create `Settings(BaseSettings)` with SettingsConfigDict(env_file=".env", env_file_encoding="utf-8", case_sensitive=False)
    - Fields: APP_ENV (str, default "development"), DEBUG (bool, default False), API_V1_PREFIX (str, default "/api/v1"), BACKEND_PORT (int, default 8060)
    - DATABASE_URL: PostgresDsn (required, no default)
    - REDIS_URL: str = "redis://localhost:6379/0"
    - JWT_PRIVATE_KEY: SecretStr (required — declared now, used in Phase 2)
    - JWT_PUBLIC_KEY: SecretStr (required)
    - SUPER_ADMIN_EMAIL: str (required)
    - SUPER_ADMIN_PASSWORD: SecretStr (required)
    - ALLOWED_ORIGINS: list[str] = ["http://localhost:3060"]
    - Module-level singleton: `settings = Settings()` — raises ValidationError at import if env vars missing
    - IMPORTANT: For DATABASE_URL, use `str(settings.DATABASE_URL)` when passing to SQLAlchemy — Pydantic v2 returns a Url object, not a string.

    **backend/src/wxcode_adm/db/base.py:**
    - NAMING_CONVENTION dict for ix, uq, ck, fk, pk (see research Pattern 3)
    - `class Base(AsyncAttrs, DeclarativeBase)` with metadata=MetaData(naming_convention=NAMING_CONVENTION) and type_annotation_map={datetime: DateTime(timezone=True)}
    - `class TimestampMixin` with: id (Mapped[uuid.UUID], primary_key, default=uuid.uuid4, server_default=text("gen_random_uuid()")), created_at (Mapped[datetime], DateTime(timezone=True), server_default=text("now()")), updated_at (Mapped[datetime], DateTime(timezone=True), server_default=text("now()"), onupdate=datetime.utcnow)

    **backend/src/wxcode_adm/db/engine.py:**
    - Import settings from config (use lazy import or str() conversion for DATABASE_URL)
    - create_async_engine with: str(settings.DATABASE_URL), echo=settings.DEBUG, pool_pre_ping=True, pool_size=10, max_overflow=20
    - async_session_maker with: engine, class_=AsyncSession, expire_on_commit=False (MANDATORY for async)

    **backend/src/wxcode_adm/db/tenant.py:**
    - `class TenantModel(TimestampMixin, Base)`: __abstract__ = True, tenant_id: Mapped[Optional[uuid.UUID]] = mapped_column(index=True, nullable=True) — NULL means platform-owned data
    - `class SoftDeleteMixin`: deleted_at: Mapped[Optional[datetime]] = mapped_column(DateTime(timezone=True), nullable=True, default=None)
    - `install_tenant_guard(session_factory)` function: registers `do_orm_execute` event on session_factory.class_ that checks if queried entities are TenantModel subclasses. In Phase 1, the guard logs a WARNING (not raises) when an unguarded query is detected — per research recommendation. Comment clearly that this upgrades to hard raise in Phase 3.
    - Use `import logging; logger = logging.getLogger(__name__)` for the warning.
    - The guard checks `orm_execute_state.is_select` and skips column/relationship loads. For each mapper, if `issubclass(mapper.class_, TenantModel)`, check for `_tenant_enforced` execution option. If missing, log warning.

    **backend/src/wxcode_adm/common/exceptions.py:**
    - `class AppError(Exception)` with error_code (str), message (str), status_code (int, default 500)
    - `class NotFoundError(AppError)` with status_code=404
    - `class ForbiddenError(AppError)` with status_code=403
    - `class ConflictError(AppError)` with status_code=409
    - `class TenantIsolationError(AppError)` with status_code=500, error_code="TENANT_ISOLATION_VIOLATION"
    - These are domain exceptions, NOT HTTPException subclasses. They will be caught by a FastAPI exception handler in Plan 02.

    **Alembic setup:**
    - Run `cd backend && alembic init -t async alembic` to generate the async template
    - Edit `backend/alembic.ini`: set script_location = alembic (keep placeholder sqlalchemy.url — overridden in env.py)
    - Edit `backend/alembic/env.py`:
      - Import settings from wxcode_adm.config
      - Import Base from wxcode_adm.db.base
      - Override URL: config.set_main_option("sqlalchemy.url", str(settings.DATABASE_URL))
      - Set target_metadata = Base.metadata
      - Use NullPool for migration engine (NOT for production API)
      - Include comment: "# Import model modules here as they are created in future phases"
    - Create `backend/alembic/versions/.gitkeep` to track empty versions directory
    - Do NOT generate an initial migration yet (no table models exist in Phase 1)
  </action>
  <verify>
    Run with a test .env: `cd /Users/gilberto/projetos/wxk/wxcode-adm/backend && cp .env.example .env` (fill in dummy JWT keys for validation).
    Run `python -c "from wxcode_adm.config import settings; print(settings.APP_ENV)"` — must print "development".
    Run `python -c "from wxcode_adm.db.base import Base, TimestampMixin; print(Base.metadata.naming_convention)"` — must print the naming convention dict.
    Run `python -c "from wxcode_adm.db.tenant import TenantModel; print(TenantModel.__abstract__)"` — must print True.
    Run `python -c "from wxcode_adm.db.engine import engine, async_session_maker; print(type(engine).__name__)"` — must print "AsyncEngine".
    Run `python -c "from wxcode_adm.common.exceptions import AppError, TenantIsolationError; print('ok')"` — must print "ok".
    Verify alembic.ini exists and env.py imports settings.
  </verify>
  <done>pydantic-settings singleton loads and validates config at import time. SQLAlchemy async engine is configured with asyncpg, pool_pre_ping, expire_on_commit=False. Base has naming conventions and TimestampMixin. TenantModel has tenant_id column and do_orm_execute guard (logged warning). SoftDeleteMixin exists. Shared exceptions defined. Alembic initialized with async template and env.py reads from settings.</done>
</task>

</tasks>

<verification>
1. `pip install -e ".[dev]"` succeeds in backend/
2. `python -c "from wxcode_adm.config import settings"` works with .env present, raises without .env
3. `python -c "from wxcode_adm.db.engine import engine"` creates AsyncEngine
4. `python -c "from wxcode_adm.db.tenant import TenantModel, install_tenant_guard"` imports without error
5. `alembic current` runs without error (shows empty — no migrations yet)
6. All domain directories exist: auth/, tenants/, billing/, users/, common/, db/, tasks/
</verification>

<success_criteria>
- Installable Python package with all Phase 1 dependencies
- pydantic-settings loads config and fails fast on missing env vars
- SQLAlchemy 2.0 async engine configured correctly for PostgreSQL
- TenantModel base class with tenant_id and guard scaffold
- Alembic async template initialized
- Domain module directories established
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
