---
phase: 11-billing-integration-fixes
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/wxcode_adm/billing/service.py
  - backend/src/wxcode_adm/billing/router.py
  - backend/tests/test_billing.py
autonomous: true
requirements:
  - BILL-01
  - BILL-03
  - BILL-05
gap_closure: true

must_haves:
  truths:
    - "When _handle_payment_failed runs, active access tokens for all tenant members are immediately invalidated via UserSession.access_token_jti blacklisting in Redis"
    - "A regular-audience JWT (even with is_superuser=True) is rejected with 401 on all billing admin endpoints"
    - "All SC1 billing admin tests use admin-audience JWTs obtained via /api/v1/admin/login, not regular-audience JWTs"
    - "Integration test proves E2E flow #8: payment failure webhook causes subscription PAST_DUE, access token blacklisted, member blocked on platform endpoints"
  artifacts:
    - path: "backend/src/wxcode_adm/billing/service.py"
      provides: "Fixed _handle_payment_failed using blacklist_jti + UserSession.access_token_jti"
      contains: "blacklist_jti"
    - path: "backend/src/wxcode_adm/billing/router.py"
      provides: "Admin routes using require_admin instead of local require_superuser"
      contains: "require_admin"
    - path: "backend/tests/test_billing.py"
      provides: "E2E integration test for flow #8, admin-audience SC1 tests, and updated JWT audience rejection test"
      contains: "test_payment_failed_blacklists_access_token"
  key_links:
    - from: "backend/src/wxcode_adm/billing/service.py"
      to: "wxcode_adm.auth.service.blacklist_jti"
      via: "lazy import inside _handle_payment_failed"
      pattern: "blacklist_jti\\(redis_client"
    - from: "backend/src/wxcode_adm/billing/service.py"
      to: "wxcode_adm.auth.models.UserSession"
      via: "lazy import inside _handle_payment_failed"
      pattern: "UserSession\\.access_token_jti"
    - from: "backend/src/wxcode_adm/billing/router.py"
      to: "wxcode_adm.admin.dependencies.require_admin"
      via: "top-level import replacing local require_superuser"
      pattern: "from wxcode_adm\\.admin\\.dependencies import require_admin"
    - from: "backend/tests/test_billing.py"
      to: "/api/v1/admin/login"
      via: "_admin_login helper for SC1 tests"
      pattern: "_admin_login"
---

<objective>
Fix two billing integration gaps found in the v1.0 audit: (1) _handle_payment_failed uses the wrong Redis key for token blacklisting (token.token instead of UserSession.access_token_jti), and (2) billing admin routes use a local require_superuser that accepts regular-audience JWTs instead of require_admin which enforces admin audience isolation.

Purpose: Close INT-01 (critical: payment failure does not actually revoke access tokens) and INT-02 (medium: billing admin endpoints accessible with regular JWT). These are the last two integration gaps blocking v1.0 certification.
Output: Two surgical code fixes in existing files, plus updated SC1 tests to use admin-audience JWTs, plus an E2E integration test covering flow #8 (payment failure -> token blacklisted -> member blocked).
</objective>

<execution_context>
@/Users/gilberto/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gilberto/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-billing-integration-fixes/11-RESEARCH.md

# Source files being modified
@backend/src/wxcode_adm/billing/service.py
@backend/src/wxcode_adm/billing/router.py
@backend/tests/test_billing.py

# Reference patterns (canonical implementations to mirror)
@backend/src/wxcode_adm/admin/service.py
@backend/src/wxcode_adm/admin/dependencies.py
@backend/src/wxcode_adm/auth/service.py
@backend/tests/test_super_admin.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix _handle_payment_failed blacklist bug and replace require_superuser with require_admin</name>
  <files>
    backend/src/wxcode_adm/billing/service.py
    backend/src/wxcode_adm/billing/router.py
  </files>
  <action>
**INT-01 fix in billing/service.py (_handle_payment_failed, around lines 865-883):**

Inside the `if user_ids:` block, REPLACE the broken Redis blacklisting section (lines 866-879) with the correct pattern from admin/service.py:suspend_tenant:

1. Add two lazy imports inside the `if user_ids:` block (same noqa style as lines 832-838):
   - `from wxcode_adm.auth.models import UserSession  # noqa: PLC0415`
   - `from wxcode_adm.auth.service import blacklist_jti  # noqa: PLC0415`

2. Query UserSession.access_token_jti for the affected user_ids:
   ```python
   session_result = await db.execute(
       select(UserSession.access_token_jti).where(
           UserSession.user_id.in_(user_ids)
       )
   )
   jtis = [row[0] for row in session_result.fetchall()]
   for jti in jtis:
       await blacklist_jti(redis_client, jti)
   ```

3. KEEP the existing RefreshToken deletion loop (lines 881-883) — it is correct. Only replace the Redis setex block (lines 866-879) with the UserSession query + blacklist_jti loop above.

4. REMOVE the now-unused lines:
   - `ttl_seconds = _settings.ACCESS_TOKEN_TTL_HOURS * 3600` (line 873)
   - The `for token in refresh_tokens: await redis_client.setex(...)` loop (lines 874-879)

5. The existing `tokens_result` query for RefreshToken (lines 867-870) and the deletion loop (lines 881-883) stay as-is.

6. Do NOT change the function signature — `_handle_payment_failed(db, data_object)` remains unchanged.

7. The `_settings` lazy import on line 836 can be removed if no other code in the function uses it (check: it may be used elsewhere in the function for email config). If used elsewhere, keep it.

**INT-02 fix in billing/router.py:**

1. Add import at top of file (after existing imports, around line 39):
   ```python
   from wxcode_adm.admin.dependencies import require_admin
   ```

2. Replace ALL 5 occurrences of `Depends(require_superuser)` with `Depends(require_admin)`:
   - Line 82: create_plan
   - Line 107: update_plan
   - Line 130: delete_plan
   - Line 152: list_plans_admin
   - Line 167: get_plan

3. Remove the local `require_superuser` function definition (lines 46-60).

4. Remove the `ForbiddenError` import from line 36 (`from wxcode_adm.common.exceptions import ForbiddenError`) IF it is not used anywhere else in the file. Check the rest of the file first — `require_billing_access` may also raise ForbiddenError.

5. Remove the `require_verified` import from line 25 IF it is not used anywhere else in the file. Check: it may be used by other endpoints in the billing_router (not just billing_admin_router).

6. Update the module docstring (lines 1-14) to reflect that billing admin routes now use `require_admin` (admin JWT audience) instead of `require_verified + is_superuser check`.
  </action>
  <verify>
Run `cd /Users/gilberto/projetos/wxk/wxcode-adm && python -m pytest backend/tests/ --ignore=backend/tests/test_billing.py -x -q` — all non-billing tests must pass (confirms no cross-domain regressions from the code changes). Billing tests are expected to break at this point because SC1 tests still use regular-audience JWTs which require_admin now rejects — those will be fixed in Task 2.
  </verify>
  <done>
billing/service.py:_handle_payment_failed uses UserSession.access_token_jti + blacklist_jti(redis_client, jti) instead of redis_client.setex with token.token. billing/router.py uses require_admin on all 5 admin endpoints and local require_superuser is removed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update all broken SC1 tests to use admin-audience JWTs and add E2E flow #8 test</name>
  <files>
    backend/tests/test_billing.py
  </files>
  <action>
**Add two new helpers to test_billing.py (after the existing `_make_superuser` helper, around line 118):**

1. Add `_seed_super_admin` helper — mirrors the pattern from test_super_admin.py:
   ```python
   async def _seed_super_admin(test_db, email: str, password: str) -> None:
       """Insert a super-admin user directly into test DB."""
       from sqlalchemy import select
       from wxcode_adm.auth.models import User
       from wxcode_adm.auth.password import hash_password

       async with test_db() as session:
           result = await session.execute(select(User).where(User.email == email))
           existing = result.scalar_one_or_none()
           if existing is not None:
               return
           admin = User(
               email=email,
               password_hash=hash_password(password),
               email_verified=True,
               is_active=True,
               is_superuser=True,
           )
           session.add(admin)
           await session.commit()
   ```

2. Add `_admin_login` helper — mirrors the pattern from test_super_admin.py:
   ```python
   async def _admin_login(client, email: str, password: str) -> str:
       """Log in via /api/v1/admin/login and return admin-audience access token."""
       r = await client.post(
           "/api/v1/admin/login",
           json={"email": email, "password": password},
       )
       assert r.status_code == 200, f"Admin login failed: {r.text}"
       return r.json()["access_token"]
   ```

**Update 4 SC1 tests to use admin-audience JWTs:**

All 4 tests currently use `_signup_verify_login` (regular-audience JWT) + `_make_superuser` (DB flag). After INT-02, `require_admin` rejects regular-audience tokens with 401 regardless of is_superuser. Each test must be updated to use `_seed_super_admin` + `_admin_login` instead.

3. **test_superadmin_create_plan** (line ~125): Replace:
   ```python
   token, _ = await _signup_verify_login(c, redis, "admin_create@test.com")
   await _make_superuser(test_db, "admin_create@test.com")
   ```
   With:
   ```python
   await _seed_super_admin(test_db, "admin_create@test.com", "Test1234!")
   token = await _admin_login(c, "admin_create@test.com", "Test1234!")
   ```

4. **test_superadmin_update_plan** (line ~170): Same pattern — replace `_signup_verify_login` + `_make_superuser` with `_seed_super_admin` + `_admin_login`. Use email "admin_update@test.com" and password "Test1234!".

5. **test_superadmin_delete_plan** (line ~202): Same pattern — replace with `_seed_super_admin` + `_admin_login`. Use email "admin_delete@test.com" and password "Test1234!".

6. **test_list_active_plans** (line ~231): Same pattern — replace with `_seed_super_admin` + `_admin_login`. Use email "listplans@test.com" and password "Test1234!". NOTE: This test also calls the public `GET /api/v1/billing/plans` endpoint at the end (line ~258) which uses `require_verified` (not `require_admin`). That call still needs a regular-audience token. Either:
   - Use the admin token for admin CRUD operations (create, delete) and a separate regular-audience token for the public listing, OR
   - Check if `GET /api/v1/billing/plans` also accepts admin-audience tokens (it uses `require_verified` which calls `decode_access_token` — admin tokens may fail there). If so, add a second user via `_signup_verify_login` for the public listing check. The simplest fix: seed the admin user, use admin token for the admin CRUD calls, then also `_signup_verify_login` a regular user and use that token for the final `GET /api/v1/billing/plans` assertion.

**Update test_nonsuperadmin_cannot_create_plan (around line 152):**

7. Change the assertion from `assert resp.status_code == 403` to `assert resp.status_code == 401`
8. Update the docstring from "Non-super-admin gets 403" to "Regular-audience JWT gets 401 on admin billing endpoints (audience isolation)"
9. Rename the test function to `test_regular_jwt_rejected_on_billing_admin` to accurately describe the behavior being tested.

**Add new E2E integration test `test_payment_failed_blacklists_access_token`:**

Add after test_webhook_payment_failed (around line 430). Follow the same setup pattern as test_webhook_payment_failed and the session invalidation pattern from test_super_admin.py:test_suspend_tenant_invalidates_sessions.

The test must:

10. **Setup:** Unpack `c, redis, app, test_db = client`. Seed a paid plan via `_seed_paid_plan(test_db)`.

11. **Login:** Call `_signup_verify_login(c, redis, "flow8_e2e@test.com")` to get `token`. Call `_create_workspace(c, token, "Flow 8 E2E")` to get `tenant_id`.

12. **Verify pre-condition:** Make a GET request to `/api/v1/billing/subscription` with the token and X-Tenant-ID header. Assert 200 — the user can access the platform.

13. **Activate subscription:** Call `process_stripe_event(ctx, "evt_flow8_checkout", "checkout.session.completed", {"customer": "cus_flow8", "subscription": "sub_flow8", "metadata": {"tenant_id": tenant_id, "plan_id": str(paid_plan_id)}})` with `ctx = {"session_maker": test_db}`.

14. **Fire payment_failed:** Call `process_stripe_event(ctx, "evt_flow8_failed", "invoice.payment_failed", {"subscription": "sub_flow8"})`.

15. **Verify subscription is PAST_DUE:** Query TenantSubscription in test_db, assert `status == SubscriptionStatus.PAST_DUE`.

16. **Verify JTI is blacklisted in Redis:** Use `redis.scan_iter("auth:blacklist:jti:*")` and assert at least one key exists. (Alternatively, query UserSession for the user's access_token_jti and check `redis.get(f"auth:blacklist:jti:{jti}")` is not None.)

17. **Verify token is rejected:** Make the same GET request to `/api/v1/billing/subscription` with the SAME original token and X-Tenant-ID. Assert `status_code == 401` — the blacklisted JTI causes get_current_user to reject the token.

Import `process_stripe_event` from `wxcode_adm.billing.service`, `SubscriptionStatus` and `TenantSubscription` from `wxcode_adm.billing.models`, `select` from `sqlalchemy`. Use `uuid.UUID(tenant_id)` for the DB query (same pattern as test_webhook_payment_failed).

The test docstring must clearly state: "E2E flow #8: payment failure webhook -> subscription PAST_DUE -> access tokens blacklisted -> member blocked on platform-level endpoints."
  </action>
  <verify>
Run `cd /Users/gilberto/projetos/wxk/wxcode-adm && python -m pytest backend/tests/test_billing.py -x -q -v` — all billing tests pass including the 4 updated SC1 tests, the renamed test_regular_jwt_rejected_on_billing_admin, and the new test_payment_failed_blacklists_access_token. Then run the full suite: `python -m pytest backend/tests/ -x -q` — all tests pass (should be 149 or 150 tests total, 0 failures).
  </verify>
  <done>
All 5 SC1 billing admin tests updated: 4 superadmin tests now use _seed_super_admin + _admin_login for admin-audience JWTs (instead of _signup_verify_login + _make_superuser), and test_nonsuperadmin_cannot_create_plan renamed to test_regular_jwt_rejected_on_billing_admin expecting 401. New test_payment_failed_blacklists_access_token verifies the complete E2E flow #8: payment failure webhook -> PAST_DUE -> JTI blacklisted in Redis -> original access token rejected with 401. Full test suite passes with no regressions.
  </done>
</task>

</tasks>

<verification>
1. `python -m pytest backend/tests/ -x -q` — full suite passes (149+ tests, 0 failures)
2. `grep -n "blacklist_jti" backend/src/wxcode_adm/billing/service.py` — confirms blacklist_jti is called in _handle_payment_failed
3. `grep -n "require_admin" backend/src/wxcode_adm/billing/router.py` — confirms require_admin is used on admin endpoints
4. `grep -n "require_superuser" backend/src/wxcode_adm/billing/router.py` — returns no matches (dead code removed)
5. `grep -n "test_payment_failed_blacklists_access_token" backend/tests/test_billing.py` — confirms E2E test exists
6. `grep -n "_admin_login" backend/tests/test_billing.py` — confirms admin-audience login helper exists
7. `grep -n "_seed_super_admin" backend/tests/test_billing.py` — confirms superadmin seeding helper exists
</verification>

<success_criteria>
- _handle_payment_failed queries UserSession.access_token_jti and calls blacklist_jti(redis_client, jti) for each affected user
- All 5 billing admin endpoints use require_admin (admin-audience JWT enforcement)
- Local require_superuser function removed from billing/router.py
- All 4 SC1 superadmin tests (create, update, delete, list) use admin-audience JWTs via _seed_super_admin + _admin_login
- test_regular_jwt_rejected_on_billing_admin expects 401 (audience rejection) instead of 403 (forbidden)
- E2E integration test proves: payment_failed webhook -> PAST_DUE status -> JTI in Redis blacklist -> HTTP request with original token returns 401
- Full test suite passes with zero regressions
</success_criteria>

<output>
After completion, create `.planning/phases/11-billing-integration-fixes/11-01-SUMMARY.md`
</output>
