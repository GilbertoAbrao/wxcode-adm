---
phase: 19-ui-polish-and-tech-debt-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/layout/Sidebar.tsx
  - frontend/src/app/(app)/page.tsx
  - frontend/src/app/admin/login/page.tsx
  - frontend/src/providers/admin-auth-provider.tsx
  - frontend/src/app/admin/users/page.tsx
autonomous: true
requirements: []
gap_closure: true

must_haves:
  truths:
    - "Clicking every sidebar icon navigates to a valid page — no 404 reachable from sidebar"
    - "Main dashboard at / shows tenant name, plan status, member count, and subscription renewal date from live API data"
    - "After admin login, admin lands on /admin/dashboard"
    - "admin/users/page.tsx imports all design system components from @/components/ui barrel"
  artifacts:
    - path: "frontend/src/components/layout/Sidebar.tsx"
      provides: "Sidebar navigation without dead /settings link"
      contains: "navItems"
    - path: "frontend/src/app/(app)/page.tsx"
      provides: "Live tenant dashboard with real data"
      contains: "useMyTenants"
    - path: "frontend/src/app/admin/login/page.tsx"
      provides: "Admin login redirecting to /admin/dashboard"
      contains: "/admin/dashboard"
    - path: "frontend/src/providers/admin-auth-provider.tsx"
      provides: "Admin auth provider redirecting authenticated admins to /admin/dashboard"
      contains: "/admin/dashboard"
    - path: "frontend/src/app/admin/users/page.tsx"
      provides: "Admin users page with barrel imports"
      contains: "from \"@/components/ui\""
  key_links:
    - from: "frontend/src/components/layout/Sidebar.tsx"
      to: "navItems array"
      via: "No /settings entry in navItems, no Settings link in bottom section"
      pattern: "navItems"
    - from: "frontend/src/app/(app)/page.tsx"
      to: "frontend/src/hooks/useTenant.ts"
      via: "useMyTenants hook for tenant data"
      pattern: "useMyTenants"
    - from: "frontend/src/app/admin/login/page.tsx"
      to: "/admin/dashboard"
      via: "router.push on login success"
      pattern: "router\\.push.*admin/dashboard"
    - from: "frontend/src/providers/admin-auth-provider.tsx"
      to: "/admin/dashboard"
      via: "redirect for authenticated admin on public path"
      pattern: "router\\.push.*admin/dashboard"
---

<objective>
Fix four polish/tech-debt items from the v2.0 milestone audit: remove the dead sidebar settings link, replace the static placeholder dashboard with live tenant data, update admin post-login redirect to /admin/dashboard, and normalize admin/users imports to use the barrel export.

Purpose: Complete the v2.0 polish pass — every navigation path works, every page shows real data, and code patterns are consistent.
Output: Five updated files with zero dead links, a live dashboard, correct admin redirect, and consistent imports.
</objective>

<execution_context>
@/Users/gilberto/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gilberto/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@frontend/src/components/layout/Sidebar.tsx
@frontend/src/app/(app)/page.tsx
@frontend/src/app/admin/login/page.tsx
@frontend/src/providers/admin-auth-provider.tsx
@frontend/src/app/admin/users/page.tsx
@frontend/src/hooks/useTenant.ts
@frontend/src/hooks/useBilling.ts
@frontend/src/hooks/useUserAccount.ts
@frontend/src/providers/auth-provider.tsx
@frontend/src/components/ui/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove dead settings link and replace static dashboard with live data</name>
  <files>
    frontend/src/components/layout/Sidebar.tsx
    frontend/src/app/(app)/page.tsx
  </files>
  <action>
**Sidebar.tsx — Remove /settings dead link:**
1. Remove the `Settings` import from lucide-react (line 12).
2. Remove the entire bottom section `<Link href="/settings">` block (lines 73-86). Keep the bottom border-t div but only with the avatar circle (the "A" initial badge). The Settings gear icon and its wrapping Link must be completely removed so no /settings navigation exists.
3. If `Settings` is no longer used anywhere in the file after removal, also clean it from the import statement.

**page.tsx — Replace static placeholder dashboard with live tenant data:**
1. Replace the current static page with a data-driven dashboard that calls:
   - `useMyTenants()` from `@/hooks/useTenant` — to get the user's tenant list (renders first tenant's name and slug)
   - `useSubscription(tenantId)` from `@/hooks/useBilling` — to get plan name, status, and renewal date
   - `useTenantMembers(tenantId)` from `@/hooks/useTenant` — to get member count
   - `useAuthContext()` from `@/providers/auth-provider` — to get user display_name for welcome greeting
2. Extract `tenantId` from the first item in `useMyTenants().data?.tenants[0]?.id`. All tenant-scoped hooks use `enabled: !!tenantId`.
3. The four stat cards should show:
   - **Workspace**: tenant name (from useMyTenants), subtitle: tenant slug
   - **Plan**: plan name (from useSubscription), subtitle: status badge (active/free/past_due)
   - **Members**: member count (from useTenantMembers), subtitle: "team members"
   - **Renewal**: formatted current_period_end date (from useSubscription), subtitle: "next billing date" (or "No renewal" if null/free)
4. Replace the placeholder content div with a welcome section:
   - "Welcome back, {display_name || 'there'}" heading
   - "Manage your workspace, team, and billing from the sidebar." description
5. Use `LoadingSkeleton` (SkeletonCard) for loading states. Import all components from `@/components/ui` barrel.
6. Handle empty state (no tenants): show EmptyState with "No workspace found" and a message to create one.
7. Keep the existing page header structure (LayoutDashboard icon + "Dashboard" title) but change subtitle to the tenant name or "Your workspace overview".
8. Remove the "Get Started" GlowButton from the header — it had no action wired.
  </action>
  <verify>
Run `cd /Users/gilberto/projetos/wxk/wxcode-adm/frontend && npx next build 2>&1 | tail -30` — build succeeds with no errors. Grep Sidebar.tsx for "/settings" — zero matches. Grep page.tsx for "useMyTenants" — at least one match.
  </verify>
  <done>
Sidebar has no /settings link (Settings icon removed from bottom section). Dashboard page imports useMyTenants, useSubscription, useTenantMembers, useAuthContext and renders four live stat cards. No hardcoded "—" values remain. No "Phases 13-17" placeholder text remains.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix admin post-login redirect and normalize barrel imports</name>
  <files>
    frontend/src/app/admin/login/page.tsx
    frontend/src/providers/admin-auth-provider.tsx
    frontend/src/app/admin/users/page.tsx
  </files>
  <action>
**admin/login/page.tsx — Change redirect target:**
1. Line 73: Change `router.push("/admin/tenants")` to `router.push("/admin/dashboard")`.
2. Update the module docstring (line 8) from "redirects to /admin/tenants" to "redirects to /admin/dashboard".
3. Also fix the imports on lines 26-27: change individual path imports to barrel imports:
   - `import { GlowButton } from "@/components/ui/GlowButton"` and `import { GlowInput } from "@/components/ui/GlowInput"` should become a single `import { GlowButton, GlowInput } from "@/components/ui"`.

**admin-auth-provider.tsx — Change redirect target:**
1. Line 98: Change `router.push("/admin/tenants")` to `router.push("/admin/dashboard")`.
2. Update the module docstring (line 10) from "Authenticated admin on /admin/login -> redirect to /admin/tenants" to "Authenticated admin on /admin/login -> redirect to /admin/dashboard".

**admin/users/page.tsx — Normalize imports to barrel:**
1. Replace the five individual imports (lines 29-36):
   ```
   import { GlowButton } from "@/components/ui/GlowButton";
   import { GlowInput } from "@/components/ui/GlowInput";
   import { SkeletonList, SkeletonTable } from "@/components/ui/LoadingSkeleton";
   import { ErrorState } from "@/components/ui/ErrorState";
   import { EmptyState } from "@/components/ui/EmptyState";
   ```
   With a single barrel import:
   ```
   import {
     GlowButton,
     GlowInput,
     SkeletonList,
     SkeletonTable,
     ErrorState,
     EmptyState,
   } from "@/components/ui";
   ```
2. No other changes — the rest of the file remains exactly as-is.
  </action>
  <verify>
Run `cd /Users/gilberto/projetos/wxk/wxcode-adm/frontend && npx next build 2>&1 | tail -30` — build succeeds. Grep admin/login/page.tsx for "/admin/tenants" — zero matches. Grep admin-auth-provider.tsx for "/admin/tenants" — zero matches. Grep admin/users/page.tsx for `from "@/components/ui/"` (with trailing slash) — zero matches.
  </verify>
  <done>
Admin login page and admin auth provider both redirect to /admin/dashboard. admin/users/page.tsx imports all design system components from the @/components/ui barrel export, consistent with every other page in the project. admin/login/page.tsx also uses barrel imports.
  </done>
</task>

</tasks>

<verification>
1. `cd /Users/gilberto/projetos/wxk/wxcode-adm/frontend && npx next build` succeeds with no errors
2. `grep -r "/settings" frontend/src/components/layout/Sidebar.tsx` returns nothing
3. `grep -r "useMyTenants" frontend/src/app/\(app\)/page.tsx` returns at least one match
4. `grep -r "/admin/tenants" frontend/src/app/admin/login/page.tsx frontend/src/providers/admin-auth-provider.tsx` returns nothing
5. `grep -r 'from "@/components/ui/' frontend/src/app/admin/users/page.tsx` returns nothing (no individual path imports)
6. `grep -r 'from "@/components/ui"' frontend/src/app/admin/users/page.tsx` returns a match (barrel import present)
</verification>

<success_criteria>
- No 404 reachable from sidebar navigation (settings link removed)
- Dashboard at / renders live tenant data (workspace name, plan, members, renewal) via API hooks
- Admin login redirects to /admin/dashboard (both login page and auth provider)
- admin/users/page.tsx uses barrel import from @/components/ui
- Next.js build passes with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/19-ui-polish-and-tech-debt-cleanup/19-01-SUMMARY.md`
</output>
