---
phase: 13-auth-flows-ui
plan: 03
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - frontend/src/app/(auth)/verify-email/page.tsx
  - frontend/src/app/(auth)/forgot-password/page.tsx
  - frontend/src/app/(auth)/reset-password/page.tsx
autonomous: true
requirements: [AUI-03, AUI-04]

must_haves:
  truths:
    - "User arriving at /verify-email?email=... sees a 6-digit OTP input and can submit the code"
    - "After successful email verification, user is redirected to /onboarding (first time) or /login (returning)"
    - "User can request a 'resend code' on the verification page"
    - "User can fill in their email at /forgot-password and submit a reset request"
    - "User arriving at /reset-password?token=... can enter a new password with confirmation"
    - "Password reset form enforces password match between new_password and confirm_password"
    - "After successful password reset, user is redirected to /login with a success message"
  artifacts:
    - path: "frontend/src/app/(auth)/verify-email/page.tsx"
      provides: "Email verification page with 6-digit OTP input, resend button, redirect on success"
      min_lines: 60
    - path: "frontend/src/app/(auth)/forgot-password/page.tsx"
      provides: "Password reset request page with email input"
      min_lines: 40
    - path: "frontend/src/app/(auth)/reset-password/page.tsx"
      provides: "Password reset confirm page with new password + confirmation fields"
      min_lines: 60
  key_links:
    - from: "frontend/src/app/(auth)/verify-email/page.tsx"
      to: "frontend/src/hooks/useAuth.ts"
      via: "useVerifyEmail and useResendVerification mutations"
      pattern: "useVerifyEmail|useResendVerification"
    - from: "frontend/src/app/(auth)/forgot-password/page.tsx"
      to: "frontend/src/hooks/useAuth.ts"
      via: "useForgotPassword mutation"
      pattern: "useForgotPassword"
    - from: "frontend/src/app/(auth)/reset-password/page.tsx"
      to: "frontend/src/hooks/useAuth.ts"
      via: "useResetPassword mutation"
      pattern: "useResetPassword"
---

<objective>
Create the email verification page (OTP input after signup), forgot password request page, and password reset confirm page.

Purpose: These are the secondary auth flows that complete account activation (email verify) and account recovery (password reset). Both are essential for a functional auth system.
Output: /verify-email, /forgot-password, and /reset-password pages with forms, validation, and API integration.
</objective>

<execution_context>
@/Users/gilberto/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gilberto/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-auth-flows-ui/13-01-SUMMARY.md
@frontend/src/hooks/useAuth.ts
@frontend/src/lib/validations.ts
@frontend/src/components/ui/GlowButton.tsx
@frontend/src/components/ui/GlowInput.tsx
@frontend/src/app/(auth)/layout.tsx
@backend/src/wxcode_adm/auth/schemas.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create email verification page with OTP input</name>
  <files>
    frontend/src/app/(auth)/verify-email/page.tsx
  </files>
  <action>
1. Create `frontend/src/app/(auth)/verify-email/page.tsx`:
   - "use client" directive
   - Import useSearchParams from next/navigation to read `?email=` query param
   - Import useRouter from next/navigation
   - Import useVerifyEmail, useResendVerification from @/hooks/useAuth
   - Import react-hook-form with zodResolver(verifyEmailSchema) from validations.ts
   - Import GlowButton, GlowInput from @/components/ui
   - Import { KeyRound, RotateCcw } from lucide-react

   - Read email from searchParams.get("email") — if missing, redirect to /signup
   - Display: heading "Verify your email", subtitle "We sent a 6-digit code to {email}"

   - Form:
     - Single GlowInput for the 6-digit code: type="text", inputMode="numeric", maxLength={6}, placeholder="000000", autoFocus, leftIcon={KeyRound}, centered text (text-center tracking-widest text-lg font-mono)
     - Submit GlowButton "Verify Email"

   - onSubmit: call verifyEmailMutation.mutate({ email, code: data.code })
     - onSuccess: redirect to `/onboarding` (new users always go to onboarding after verification)
     - onError: show API error inline (e.g., "Invalid or expired code")

   - Resend section below form:
     - "Didn't receive the code?" text
     - GlowButton variant="ghost" "Resend Code" — calls resendMutation.mutate({ email })
     - After resend success, show "New code sent!" message and disable button for 60 seconds (countdown timer using setInterval)
     - Resend button shows cooldown: "Resend in {seconds}s" when disabled

   - Styling: same card pattern as signup/login (max-w-sm, border-zinc-800, bg-obsidian-900/50)
  </action>
  <verify>
    `cd /Users/gilberto/projetos/wxk/wxcode-adm/frontend && pnpm build` passes. `ls src/app/\(auth\)/verify-email/page.tsx` exists.
  </verify>
  <done>
    /verify-email page shows OTP input, reads email from query params, submits to POST /auth/verify-email, has resend button with 60s cooldown, redirects to /onboarding on success.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create forgot password and reset password pages</name>
  <files>
    frontend/src/app/(auth)/forgot-password/page.tsx
    frontend/src/app/(auth)/reset-password/page.tsx
  </files>
  <action>
1. Create `frontend/src/app/(auth)/forgot-password/page.tsx`:
   - "use client" directive
   - Import react-hook-form with zodResolver(forgotPasswordSchema)
   - Import GlowButton, GlowInput from @/components/ui
   - Import useForgotPassword from @/hooks/useAuth
   - Import { Mail, ArrowLeft } from lucide-react
   - Import Link from next/link

   - Heading: "Reset your password", subtitle: "Enter your email and we'll send you a reset link"

   - Form:
     - Email GlowInput with leftIcon={Mail}
     - Submit GlowButton "Send Reset Link"

   - onSubmit: call forgotPasswordMutation.mutate({ email: data.email })
     - onSuccess: show success state — replace form with a success message: "If an account exists for {email}, we've sent a password reset link. Check your email." and a GlowButton variant="secondary" "Back to Login" linking to /login
     - This is enumeration-safe: always shows success regardless of whether email exists

   - Footer: ArrowLeft icon + "Back to Login" link to /login

   - Styling: same card pattern

2. Create `frontend/src/app/(auth)/reset-password/page.tsx`:
   - "use client" directive
   - Import useSearchParams to read `?token=` query param
   - Import useRouter from next/navigation
   - Import react-hook-form with zodResolver(resetPasswordSchema) from validations.ts
   - Import GlowButton, GlowInput from @/components/ui
   - Import useResetPassword from @/hooks/useAuth
   - Import { Lock, Eye, EyeOff } from lucide-react

   - Read token from searchParams.get("token") — if missing, show error "Invalid or missing reset link" with link back to /forgot-password

   - Heading: "Set new password"

   - Form:
     - New Password: GlowInput type={showPassword ? "text" : "password"}, leftIcon={Lock}, rightIcon toggle, register("new_password")
     - Confirm Password: GlowInput type="password", leftIcon={Lock}, register("confirm_password"), error from formState.errors.confirm_password (shows "Passwords do not match" from zod refine)
     - Submit GlowButton "Reset Password"

   - State: showPassword toggle (for new_password field only)

   - onSubmit: call resetPasswordMutation.mutate({ token, new_password: data.new_password })
     - onSuccess: redirect to `/login?reset=success` (login page can show a toast/banner for this)
     - onError: show API error (e.g., "Reset link has expired" for 401)

   - Styling: same card pattern
  </action>
  <verify>
    `cd /Users/gilberto/projetos/wxk/wxcode-adm/frontend && pnpm build` passes. `ls src/app/\(auth\)/forgot-password/page.tsx src/app/\(auth\)/reset-password/page.tsx` — both files exist.
  </verify>
  <done>
    /forgot-password page accepts email, calls POST /auth/forgot-password, shows enumeration-safe success message.
    /reset-password?token=... page has new password + confirm fields, validates match, calls POST /auth/reset-password, redirects to /login on success.
  </done>
</task>

</tasks>

<verification>
1. `pnpm build` passes with /verify-email, /forgot-password, /reset-password routes in build output
2. verify-email page reads ?email= param, shows 6-digit input, has resend with cooldown
3. forgot-password page shows success state after submission (enumeration-safe)
4. reset-password page reads ?token= param, validates password match, calls API
5. All pages use GlowButton + GlowInput consistently
6. All pages render inside (auth) layout (no sidebar)
</verification>

<success_criteria>
- /verify-email with OTP input, resend with 60s cooldown, redirect to /onboarding on success
- /forgot-password with email input, enumeration-safe success message
- /reset-password with new password + confirmation, match validation, redirect to /login
- All 3 pages use shared validation schemas from validations.ts
- All 3 pages use Obsidian Studio design system components
- Production build passes
</success_criteria>

<output>
After completion, create `.planning/phases/13-auth-flows-ui/13-03-SUMMARY.md`
</output>
