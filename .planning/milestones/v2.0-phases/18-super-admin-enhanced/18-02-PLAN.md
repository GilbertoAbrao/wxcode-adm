---
phase: 18-super-admin-enhanced
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/hooks/useAdminTenants.ts
  - frontend/src/hooks/useAdminUsers.ts
  - frontend/src/app/admin/tenants/page.tsx
  - frontend/src/app/admin/tenants/[tenantId]/page.tsx
  - frontend/src/app/admin/users/page.tsx
autonomous: true
requirements: [SAI-04, SAI-05]

must_haves:
  truths:
    - "Admin can click a tenant name in the tenant list to navigate to a detail page"
    - "Tenant detail page shows subscription status, MFA enforcement, wxcode URL, member count, and dates"
    - "Admin can click Force Password Reset in the user detail drawer, enter a reason, and trigger the reset"
    - "Force password reset calls POST /admin/users/{id}/force-reset with reason body"
  artifacts:
    - path: "frontend/src/app/admin/tenants/[tenantId]/page.tsx"
      provides: "Tenant detail page with full tenant info"
      contains: "TenantDetailResponse"
    - path: "frontend/src/hooks/useAdminTenants.ts"
      provides: "useAdminTenantDetail hook added"
      exports: ["useAdminTenantDetail"]
    - path: "frontend/src/hooks/useAdminUsers.ts"
      provides: "useForcePasswordReset mutation hook added"
      exports: ["useForcePasswordReset"]
    - path: "frontend/src/app/admin/users/page.tsx"
      provides: "Force Password Reset button in user detail drawer"
      contains: "force-reset"
  key_links:
    - from: "frontend/src/hooks/useAdminTenants.ts"
      to: "/admin/tenants/{tenant_id}"
      via: "adminApiClient GET"
      pattern: "adminApiClient.*admin/tenants/"
    - from: "frontend/src/hooks/useAdminUsers.ts"
      to: "/admin/users/{user_id}/force-reset"
      via: "adminApiClient POST"
      pattern: "adminApiClient.*force-reset"
    - from: "frontend/src/app/admin/tenants/page.tsx"
      to: "frontend/src/app/admin/tenants/[tenantId]/page.tsx"
      via: "Next.js Link href"
      pattern: "href.*admin/tenants/"
    - from: "frontend/src/app/admin/users/page.tsx"
      to: "useForcePasswordReset"
      via: "mutation in UserDetailDrawer"
      pattern: "useForcePasswordReset|force-reset"
---

<objective>
Add tenant detail page and force password reset to complete the super-admin enhanced toolkit.

Purpose: Allow admin to drill into individual tenant details from the tenant list, and to force a password reset for any user directly from the user detail drawer.
Output: New tenant detail page at /admin/tenants/[tenantId], force password reset button in the existing user detail drawer, and updated AdminNav across modified pages.
</objective>

<execution_context>
@/Users/gilberto/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gilberto/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-super-admin-ui/17-02-SUMMARY.md
@.planning/phases/17-super-admin-ui/17-03-SUMMARY.md
@frontend/src/hooks/useAdminTenants.ts
@frontend/src/hooks/useAdminUsers.ts
@frontend/src/app/admin/tenants/page.tsx
@frontend/src/app/admin/users/page.tsx
@backend/src/wxcode_adm/admin/schemas.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add tenant detail hook, force password reset hook, and tenant detail page</name>
  <files>
    frontend/src/hooks/useAdminTenants.ts
    frontend/src/hooks/useAdminUsers.ts
    frontend/src/app/admin/tenants/[tenantId]/page.tsx
  </files>
  <action>
**Modify `frontend/src/hooks/useAdminTenants.ts`:**
- Add TenantDetailResponse interface matching backend admin/schemas.py TenantDetailResponse:
  { id: string, name: string, slug: string, is_suspended: boolean, is_deleted: boolean, mfa_enforced: boolean, wxcode_url: string | null, plan_name: string | null, plan_slug: string | null, subscription_status: string | null, member_count: number, created_at: string, updated_at: string }
- Add to ADMIN_TENANT_KEYS: detail: (tenantId: string) => ["admin", "tenants", tenantId] as const
- Add useAdminTenantDetail(tenantId: string | null) hook:
  - queryKey: ADMIN_TENANT_KEYS.detail(tenantId ?? "")
  - queryFn: adminApiClient<TenantDetailResponse>(`/admin/tenants/${tenantId}`)
  - enabled: !!tenantId
  - staleTime: 30_000
- Export TenantDetailResponse and useAdminTenantDetail

**Modify `frontend/src/hooks/useAdminUsers.ts`:**
- Add ForceResetResponse interface: { message: string, user_id: string }
- Add useForcePasswordReset() mutation hook:
  - mutationFn: ({ user_id, reason }: { user_id: string, reason: string }) => adminApiClient<ForceResetResponse>(`/admin/users/${user_id}/force-reset`, { method: "POST", body: JSON.stringify({ reason }) })
  - onSuccess: invalidateQueries({ queryKey: ["admin", "users"] })
- Export useForcePasswordReset

**Create `frontend/src/app/admin/tenants/[tenantId]/page.tsx`:**
- "use client" directive
- Import useAdminTenantDetail and TenantDetailResponse from useAdminTenants hook
- Import useParams from "next/navigation" to get tenantId from URL
- Import Link from "next/link" for back navigation
- Import { ArrowLeft } from "lucide-react" for back arrow icon
- AdminNav with 4 links (Dashboard, Tenants active, Users, Audit Logs) — same pattern as Plan 01 pages
- Page structure:
  1. AdminNav with Tenants active (cyan-400)
  2. Back link: ArrowLeft + "Back to Tenants" linking to /admin/tenants
  3. Loading state: LoadingSkeleton lines={6} in a card
  4. Error state: ErrorState with retry
  5. Tenant header: tenant name (h1) + slug (mono, zinc-500) + status badge (same statusBadge pattern from tenants list)
  6. Info grid (lg:grid-cols-2 gap-6): two cards side by side
     - Left card "Subscription & Plan":
       - Plan: plan_name ?? "No plan" + plan_slug in mono
       - Status: subscription_status with colored badge (active=emerald, past_due=amber, canceled=rose, free=zinc)
       - wxcode URL: wxcode_url ?? "Not configured" — if present, show as clickable link
     - Right card "Security & Membership":
       - MFA Enforced: mfa_enforced ? "Yes" (emerald) : "No" (zinc)
       - Member Count: member_count
       - Created: formatDate(created_at)
       - Updated: formatDate(updated_at)
  - Card styling: rounded-lg border border-zinc-800 bg-zinc-900/50 p-6
  - Each info row: label (text-sm text-zinc-500) + value (text-sm text-zinc-300) in a flex or dl/dt/dd pattern
  - Status badge colors: active=emerald-400/10+emerald-400, past_due=amber-400/10+amber-400, canceled=rose-400/10+rose-400, free=zinc-400/10+zinc-400, null=zinc
  </action>
  <verify>
Run `cd /Users/gilberto/projetos/wxk/wxcode-adm/frontend && npx tsc --noEmit` — zero type errors. Verify [tenantId] directory and page.tsx exist.
  </verify>
  <done>
useAdminTenantDetail hook exists and calls GET /admin/tenants/{id}. useForcePasswordReset hook exists and calls POST /admin/users/{id}/force-reset. Tenant detail page renders at /admin/tenants/[tenantId] with subscription info, security details, and member count.
  </done>
</task>

<task type="auto">
  <name>Task 2: Link tenant names to detail page and add force password reset to user drawer</name>
  <files>
    frontend/src/app/admin/tenants/page.tsx
    frontend/src/app/admin/users/page.tsx
  </files>
  <action>
**Modify `frontend/src/app/admin/tenants/page.tsx`:**
1. Import Link from "next/link" (if not already imported)
2. In the tenant table, wrap the tenant name cell content with a Link:
   - Change `<td className="..."><span>{tenant.name}</span></td>` to `<td className="..."><Link href={`/admin/tenants/${tenant.id}`} className="text-cyan-400 hover:text-cyan-300 hover:underline transition-colors">{tenant.name}</Link></td>`
   - This makes tenant names clickable, navigating to the detail page
3. Update the AdminNav in this page to include all 4 nav links:
   - Dashboard (/admin/dashboard) — inactive style
   - Tenants (/admin/tenants) — active (cyan-400 + border-b-2)
   - Users (/admin/users) — inactive style
   - Audit Logs (/admin/audit-logs) — inactive style
   Keep the Logout button on the right as-is.

**Modify `frontend/src/app/admin/users/page.tsx`:**
1. Import useForcePasswordReset from "@/hooks/useAdminUsers"
2. Update the AdminNav in this page to include all 4 nav links:
   - Dashboard (/admin/dashboard) — inactive style
   - Tenants (/admin/tenants) — inactive style
   - Users (/admin/users) — active (cyan-400 + border-b-2)
   - Audit Logs (/admin/audit-logs) — inactive style
3. In the UserDetailDrawer component, add a "Force Password Reset" section after the existing Sessions section:
   - Section header: "Force Password Reset" (text-sm font-semibold text-zinc-300)
   - Description: "Invalidates all sessions and sends a password reset email." (text-xs text-zinc-500)
   - Inline form with:
     - GlowInput for reason (placeholder "Reason for forced reset...", fullWidth)
     - GlowButton "Force Reset" (text-rose-400 border-rose-400/30 border) — disabled until reason is not empty
     - Success message: show "Password reset initiated" in emerald-400 text for 3 seconds after success
     - Error message: show error in rose-400 text
   - The form uses useForcePasswordReset mutation:
     - On click: call mutateAsync({ user_id: selectedUser.id, reason })
     - On success: clear reason input, show success message, invalidation happens automatically via the hook
     - On error: show ApiError message
   - State management: local useState for resetReason (string) and resetStatus ("idle" | "success" | "error") with resetMessage (string)
   - After successful reset, auto-clear the success message after 3 seconds via setTimeout

CRITICAL: Do NOT remove or alter any existing functionality in users/page.tsx. Only ADD the force reset section to the drawer and update the AdminNav links. The existing search, table, drawer, block/unblock must remain unchanged.
  </action>
  <verify>
Run `cd /Users/gilberto/projetos/wxk/wxcode-adm/frontend && npx tsc --noEmit` — zero type errors. Verify tenant names in the list are wrapped in Link tags. Verify UserDetailDrawer has a "Force Password Reset" section with reason input.
  </verify>
  <done>
Tenant names in /admin/tenants are clickable links to /admin/tenants/[id]. User detail drawer in /admin/users has a "Force Password Reset" section with reason input and confirmation button. All 4 admin pages have consistent 4-link AdminNav (Dashboard, Tenants, Users, Audit Logs).
  </done>
</task>

</tasks>

<verification>
1. `cd frontend && npx tsc --noEmit` passes with zero errors
2. `cd frontend && pnpm build` completes without errors
3. Tenant detail page exists at frontend/src/app/admin/tenants/[tenantId]/page.tsx
4. Tenant names in the list page are wrapped in Next.js Link components
5. User detail drawer contains "Force Password Reset" section with reason input
6. useForcePasswordReset hook calls POST /admin/users/{id}/force-reset
7. useAdminTenantDetail hook calls GET /admin/tenants/{id}
8. All modified admin pages have consistent 4-link AdminNav
</verification>

<success_criteria>
- Clicking a tenant name in /admin/tenants navigates to /admin/tenants/[tenantId] showing full tenant details
- Tenant detail page displays subscription status, MFA enforcement, wxcode URL, member count, and dates
- Force Password Reset button in user drawer accepts a reason and calls the backend endpoint
- AdminNav is consistent across all admin pages with Dashboard, Tenants, Users, and Audit Logs links
</success_criteria>

<output>
After completion, create `.planning/phases/18-super-admin-enhanced/18-02-SUMMARY.md`
</output>
