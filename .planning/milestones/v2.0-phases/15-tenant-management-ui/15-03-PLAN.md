---
phase: 15-tenant-management-ui
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/wxcode_adm/tenants/schemas.py
  - backend/src/wxcode_adm/tenants/service.py
  - frontend/src/hooks/useTenant.ts
  - frontend/src/app/(app)/team/page.tsx
autonomous: true
requirements: [TMI-01, TMI-02, TMI-03]
gap_closure: true

must_haves:
  truths:
    - "MFA enforcement toggle reflects the persisted state on page load (not hardcoded false)"
    - "GET /tenants/me response includes mfa_enforced boolean for each tenant"
  artifacts:
    - path: "backend/src/wxcode_adm/tenants/schemas.py"
      provides: "mfa_enforced field on MyTenantItem"
      contains: "mfa_enforced: bool"
    - path: "backend/src/wxcode_adm/tenants/service.py"
      provides: "mfa_enforced value in get_user_tenants return dict"
      contains: "mfa_enforced"
    - path: "frontend/src/hooks/useTenant.ts"
      provides: "mfa_enforced field on MyTenantItem interface"
      contains: "mfa_enforced"
    - path: "frontend/src/app/(app)/team/page.tsx"
      provides: "MFA toggle initial state seeded from API data"
      contains: "useEffect"
  key_links:
    - from: "backend/src/wxcode_adm/tenants/service.py"
      to: "Tenant.mfa_enforced column"
      via: "membership.tenant.mfa_enforced in get_user_tenants dict"
      pattern: "membership\\.tenant\\.mfa_enforced"
    - from: "frontend/src/app/(app)/team/page.tsx"
      to: "frontend/src/hooks/useTenant.ts"
      via: "useMyTenants data seeds mfaEnforced state via useEffect"
      pattern: "tenantsData.*mfa_enforced"
---

<objective>
Close the single verification gap from Phase 15: the MFA enforcement toggle does not reflect the persisted backend state on page load. Currently `mfa_enforced` is absent from the `GET /tenants/me` response, so the frontend always initializes the toggle to `false`.

Purpose: Satisfy SC-3 — "the toggle reflects the current enforcement state on page load"
Output: Backend returns `mfa_enforced` in tenant list; frontend seeds toggle from API data
</objective>

<execution_context>
@/Users/gilberto/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gilberto/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-tenant-management-ui/15-VERIFICATION.md
@.planning/phases/15-tenant-management-ui/15-02-SUMMARY.md
@backend/src/wxcode_adm/tenants/schemas.py
@backend/src/wxcode_adm/tenants/service.py
@frontend/src/hooks/useTenant.ts
@frontend/src/app/(app)/team/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expose mfa_enforced in backend GET /tenants/me and seed frontend toggle from API data</name>
  <files>
    backend/src/wxcode_adm/tenants/schemas.py
    backend/src/wxcode_adm/tenants/service.py
    frontend/src/hooks/useTenant.ts
    frontend/src/app/(app)/team/page.tsx
  </files>
  <action>
    Four targeted edits across backend and frontend:

    1. **backend/src/wxcode_adm/tenants/schemas.py** (line ~170):
       Add `mfa_enforced: bool` field to the `MyTenantItem` class, after the existing `billing_access: bool` field.

    2. **backend/src/wxcode_adm/tenants/service.py** (line ~237):
       In the `get_user_tenants` function, add `"mfa_enforced": membership.tenant.mfa_enforced` to the dict comprehension that builds each tenant item. Place it after the `"billing_access"` entry. The Tenant model already has an `mfa_enforced` column (used by the PATCH /tenants/current/mfa-enforcement endpoint), and `selectinload(TenantMembership.tenant)` already eagerly loads the full Tenant object, so no additional query is needed.

    3. **frontend/src/hooks/useTenant.ts** (line ~30):
       Add `mfa_enforced?: boolean` to the `MyTenantItem` interface, after the existing `billing_access` field. Use optional (`?`) because the field is new and older cached responses may lack it.

    4. **frontend/src/app/(app)/team/page.tsx** (line ~60):
       Keep `useState(false)` as the initial value (the data is not available at first render), but add a `useEffect` that syncs the toggle state when `tenantsData` loads:
       ```typescript
       useEffect(() => {
         if (tenantsData?.tenants?.[0]?.mfa_enforced !== undefined) {
           setMfaEnforced(tenantsData.tenants[0].mfa_enforced);
         }
       }, [tenantsData]);
       ```
       Place this useEffect after the existing state declarations and before any other hooks. Import `useEffect` from React if not already imported (check the existing imports — it may already be there from other usage).

    **Do NOT** change any other behavior — the toggle interaction, PATCH mutation, Owner guard, and query invalidation all remain as-is.
  </action>
  <verify>
    1. Backend: `cd backend && python -c "from wxcode_adm.tenants.schemas import MyTenantItem; assert 'mfa_enforced' in MyTenantItem.model_fields; print('OK: schema has mfa_enforced')"` succeeds.
    2. Frontend: `cd frontend && npx tsc --noEmit` passes with no errors.
    3. Grep confirmation: `grep -n 'mfa_enforced' backend/src/wxcode_adm/tenants/schemas.py backend/src/wxcode_adm/tenants/service.py frontend/src/hooks/useTenant.ts frontend/src/app/\(app\)/team/page.tsx` shows the field in all four files.
  </verify>
  <done>
    GET /tenants/me response includes mfa_enforced boolean for each tenant item. The team page MFA enforcement toggle initializes from the API value on page load instead of hardcoded false. SC-3 ("the toggle reflects the current enforcement state on page load") is satisfied.
  </done>
</task>

</tasks>

<verification>
1. Backend schema validation: MyTenantItem includes mfa_enforced: bool
2. Backend service: get_user_tenants returns mfa_enforced from tenant model
3. Frontend interface: MyTenantItem has mfa_enforced?: boolean
4. Frontend page: useEffect syncs mfaEnforced state from tenantsData on load
5. TypeScript compilation passes without errors
</verification>

<success_criteria>
- The MFA enforcement toggle on the /team page shows the correct persisted state when the page loads (not always false)
- Toggling MFA enforcement and refreshing the page preserves the toggle position
- No regressions to existing member list, invite, role change, or remove functionality
</success_criteria>

<output>
After completion, create `.planning/phases/15-tenant-management-ui/15-03-SUMMARY.md`
</output>
