---
phase: 12-design-system-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - frontend/src/app/globals.css
  - frontend/src/lib/animations.ts
  - frontend/src/components/ui/GlowButton.tsx
  - frontend/src/components/ui/GlowInput.tsx
  - frontend/src/components/ui/LoadingSkeleton.tsx
  - frontend/src/components/ui/EmptyState.tsx
  - frontend/src/components/ui/ErrorState.tsx
  - frontend/src/components/ui/AnimatedList.tsx
  - frontend/src/components/ui/index.ts
autonomous: true
requirements:
  - DS-02

must_haves:
  truths:
    - "The Obsidian Studio dark theme is applied -- deep dark background (#09090b), correct blue accent (#3b82f6), glow utilities work"
    - "GlowButton renders with blue glow on hover, supports primary/success/danger/secondary/ghost variants"
    - "GlowInput renders with blue glow ring on focus and rose glow ring on error"
    - "LoadingSkeleton renders shimmer animation with Obsidian palette gradients"
    - "EmptyState renders with icon, title, description, and optional action button"
    - "ErrorState renders with rose-colored icon and optional retry button"
    - "AnimatedList renders children with staggered entrance animations"
    - "All 6 custom components are importable from @/components/ui barrel export"
  artifacts:
    - path: "frontend/src/app/globals.css"
      provides: "Full Obsidian Studio theme (tokens, dark mode vars, animations, scrollbar)"
      contains: "--obsidian-950"
    - path: "frontend/src/lib/animations.ts"
      provides: "Framer Motion animation variants (fadeInUp, staggerContainer, staggerItem)"
      exports: ["fadeInUp", "staggerContainer", "staggerContainerFast", "staggerContainerSlow", "staggerItem"]
    - path: "frontend/src/components/ui/GlowButton.tsx"
      provides: "Button with glow effect on hover"
      exports: ["GlowButton"]
    - path: "frontend/src/components/ui/GlowInput.tsx"
      provides: "Input with glow ring on focus"
      exports: ["GlowInput"]
    - path: "frontend/src/components/ui/LoadingSkeleton.tsx"
      provides: "Shimmer loading placeholder"
      exports: ["LoadingSkeleton", "SkeletonCard", "SkeletonList", "SkeletonTable"]
    - path: "frontend/src/components/ui/EmptyState.tsx"
      provides: "Empty state display with optional action"
      exports: ["EmptyState", "EmptySearch", "EmptyList"]
    - path: "frontend/src/components/ui/ErrorState.tsx"
      provides: "Error display with retry option"
      exports: ["ErrorState", "NetworkError", "NotFoundError", "PermissionError"]
    - path: "frontend/src/components/ui/AnimatedList.tsx"
      provides: "List wrapper with stagger animations"
      exports: ["AnimatedList", "AnimatedListItem", "AnimatedGrid", "AnimatedGridItem"]
    - path: "frontend/src/components/ui/index.ts"
      provides: "Barrel export for all UI components"
      contains: "GlowButton"
  key_links:
    - from: "frontend/src/components/ui/GlowButton.tsx"
      to: "frontend/src/lib/utils.ts"
      via: "cn() import"
      pattern: "import.*cn.*from.*@/lib/utils"
    - from: "frontend/src/components/ui/EmptyState.tsx"
      to: "frontend/src/lib/animations.ts"
      via: "fadeInUp animation variant"
      pattern: "import.*fadeInUp.*from.*@/lib/animations"
    - from: "frontend/src/components/ui/AnimatedList.tsx"
      to: "frontend/src/lib/animations.ts"
      via: "stagger animation variants"
      pattern: "import.*staggerContainer.*from.*@/lib/animations"
    - from: "frontend/src/components/ui/index.ts"
      to: "frontend/src/components/ui/*.tsx"
      via: "barrel re-exports"
      pattern: "export.*from"
---

<objective>
Port the complete Obsidian Studio theme (globals.css with all design tokens) and all 6 custom components (GlowButton, GlowInput, LoadingSkeleton, EmptyState, ErrorState, AnimatedList) from the wxcode frontend into wxcode-adm.

Purpose: Establish the visual identity and reusable component library so every subsequent UI phase (13-17) uses consistent Obsidian Studio styling without reimplementing any visual primitives.
Output: Full theme in globals.css, animation variants in lib/animations.ts, 6 custom components in components/ui/, barrel export index.
</objective>

<execution_context>
@/Users/gilberto/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gilberto/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-design-system-foundation/12-01-SUMMARY.md

# Source files to port FROM (read these for exact implementation)
@/Users/gilberto/projetos/wxk/wxcode/frontend/src/app/globals.css
@/Users/gilberto/projetos/wxk/wxcode/frontend/src/styles/tokens.css
@/Users/gilberto/projetos/wxk/wxcode/frontend/src/lib/animations.ts
@/Users/gilberto/projetos/wxk/wxcode/frontend/src/components/ui/GlowButton.tsx
@/Users/gilberto/projetos/wxk/wxcode/frontend/src/components/ui/GlowInput.tsx
@/Users/gilberto/projetos/wxk/wxcode/frontend/src/components/ui/LoadingSkeleton.tsx
@/Users/gilberto/projetos/wxk/wxcode/frontend/src/components/ui/EmptyState.tsx
@/Users/gilberto/projetos/wxk/wxcode/frontend/src/components/ui/ErrorState.tsx
@/Users/gilberto/projetos/wxk/wxcode/frontend/src/components/ui/AnimatedList.tsx
@/Users/gilberto/projetos/wxk/wxcode/frontend/src/components/ui/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port Obsidian Studio theme (globals.css) and animation variants</name>
  <files>
    frontend/src/app/globals.css
    frontend/src/lib/animations.ts
  </files>
  <action>
**globals.css** -- Replace the minimal placeholder from Plan 01 with the FULL Obsidian Studio theme ported from `/Users/gilberto/projetos/wxk/wxcode/frontend/src/app/globals.css`. Port the entire file including:

1. `@import "tailwindcss"` and `@import "tw-animate-css"` at the top
2. `:root` block with ALL Obsidian palette tokens (--obsidian-950 through --obsidian-50)
3. ALL accent color tokens (blue, green, purple, amber, rose) with their glow variants
4. Spacing tokens (--space-0 through --space-16)
5. Shadow and glow tokens (--shadow-sm through --glow-rose)
6. Transition tokens (--transition-fast through --transition-spring)
7. Z-index tokens (--z-base through --z-toast)
8. `@custom-variant dark (&:is(.dark *))` declaration
9. `@theme inline` block with all shadcn CSS variable mappings (--color-background, --color-foreground, --color-primary, etc.)
10. `:root` light mode CSS variable assignments
11. `.dark` class with all dark mode CSS variable assignments (this is the primary mode for wxcode-adm)
12. `@layer base` with border, body, scrolling, focus, and selection styles
13. Dark body grain texture overlay (::before pseudo-element)
14. `@layer utilities` with all transition utilities, glow utilities, hover glow utilities, glass effect, shimmer animation, glow-pulse animation, fade-in animation, fade-in-up animation, scale-in animation
15. Dark scrollbar styling

Port this file EXACTLY from the wxcode source. The ONLY differences should be:
- Remove the `--font-mono` if JetBrains Mono is not needed for wxcode-adm (keep Geist Mono)
- Keep `--font-sans: var(--font-geist-sans)` and `--font-mono: var(--font-geist-mono)`

**animations.ts** -- Port from `/Users/gilberto/projetos/wxk/wxcode/frontend/src/lib/animations.ts` exactly as-is:
- fadeInUp, staggerContainer, staggerContainerFast, staggerContainerSlow, staggerItem
- All typed with `Variants` from framer-motion

After writing both files, verify the dev server still starts without CSS compilation errors.
  </action>
  <verify>
1. `cd frontend && pnpm dev` starts without CSS errors
2. `grep -c "obsidian-950" frontend/src/app/globals.css` returns at least 1
3. `grep -c "glow-blue" frontend/src/app/globals.css` returns at least 1
4. `grep "fadeInUp" frontend/src/lib/animations.ts` returns a match
5. `cd frontend && pnpm build` passes
  </verify>
  <done>
globals.css contains the full Obsidian Studio theme with all design tokens, dark mode variables, animations, and utilities. animations.ts exports all framer-motion variants. Dev server compiles CSS without errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Port all 6 custom components and barrel export</name>
  <files>
    frontend/src/components/ui/GlowButton.tsx
    frontend/src/components/ui/GlowInput.tsx
    frontend/src/components/ui/LoadingSkeleton.tsx
    frontend/src/components/ui/EmptyState.tsx
    frontend/src/components/ui/ErrorState.tsx
    frontend/src/components/ui/AnimatedList.tsx
    frontend/src/components/ui/index.ts
  </files>
  <action>
Port all 6 custom components from `/Users/gilberto/projetos/wxk/wxcode/frontend/src/components/ui/`. Each file should be ported as close to the original as possible, with these adjustments:

1. **GlowButton.tsx** -- Port exactly. Uses framer-motion `motion.button` with whileHover/whileTap, Loader2 from lucide-react, cn() from @/lib/utils. 5 variants (primary, success, danger, secondary, ghost), 3 sizes (sm, md, lg). Glow effect via onMouseEnter/onMouseLeave style manipulation.

2. **GlowInput.tsx** -- Port exactly. Uses useState for focus tracking, glow ring via boxShadow style, supports label/error/hint, left/right icons, 3 sizes, 2 variants (default, filled).

3. **LoadingSkeleton.tsx** -- Port exactly. Uses animate-shimmer CSS class from globals.css. Includes compound components: SkeletonCard, SkeletonList, SkeletonTable. 6 variants (text, heading, avatar, card, button, input).

4. **EmptyState.tsx** -- Port exactly. Uses framer-motion with fadeInUp variant from @/lib/animations. Includes pre-configured: EmptySearch, EmptyList. 3 sizes (sm, md, lg).

5. **ErrorState.tsx** -- Port exactly. Uses framer-motion with fadeInUp variant from @/lib/animations. Rose-themed error display with optional retry button (RefreshCw icon with spin animation). Includes pre-configured: NetworkError, NotFoundError, PermissionError.

6. **AnimatedList.tsx** -- Port exactly. Uses framer-motion AnimatePresence with stagger variants from @/lib/animations. Includes: AnimatedList, AnimatedListItem, AnimatedGrid, AnimatedGridItem. Uses `motion(Component)` pattern for polymorphic elements.

7. **index.ts** -- Port the barrel export from wxcode frontend. Export all types and components. Also re-export animation variants from @/lib/animations.

All imports should use:
- `@/lib/utils` for cn()
- `@/lib/animations` for animation variants
- `framer-motion` for motion components and types
- `lucide-react` for icons

After creating all components, update `frontend/src/app/page.tsx` to import and render a quick showcase:
- Import GlowButton, GlowInput, LoadingSkeleton, EmptyState from @/components/ui
- Render at least one of each to verify they render without runtime errors
- This is a temporary showcase page (will be replaced by the app shell in Plan 03)

Do NOT remove or modify `frontend/src/components/ui/button.tsx` (shadcn base button from Plan 01) -- keep it alongside the custom components.
  </action>
  <verify>
1. `cd frontend && pnpm build` passes with no TypeScript errors
2. `cd frontend && pnpm dev` starts and the showcase page renders all components
3. Each component file exists:
   - `ls frontend/src/components/ui/GlowButton.tsx`
   - `ls frontend/src/components/ui/GlowInput.tsx`
   - `ls frontend/src/components/ui/LoadingSkeleton.tsx`
   - `ls frontend/src/components/ui/EmptyState.tsx`
   - `ls frontend/src/components/ui/ErrorState.tsx`
   - `ls frontend/src/components/ui/AnimatedList.tsx`
   - `ls frontend/src/components/ui/index.ts`
4. `grep "GlowButton" frontend/src/components/ui/index.ts` confirms barrel export
5. Importing `{ GlowButton, EmptyState, ErrorState } from "@/components/ui"` compiles without errors
  </verify>
  <done>
All 6 custom components (GlowButton, GlowInput, LoadingSkeleton, EmptyState, ErrorState, AnimatedList) are ported with identical API and styling. Barrel export in index.ts exposes all components and types. Build passes. A temporary showcase page confirms runtime rendering works.
  </done>
</task>

</tasks>

<verification>
1. `cd frontend && pnpm build` -- production build succeeds with all theme + components
2. `cd frontend && pnpm dev` -- dev server shows components rendering with Obsidian dark theme
3. All 6 components importable from `@/components/ui`
4. globals.css contains the full Obsidian Studio token set (obsidian palette, accent colors, glows, animations)
5. No TypeScript errors in any component file
</verification>

<success_criteria>
- Full Obsidian Studio theme applied -- deep dark background (#09090b), blue accent (#3b82f6), glow effects work
- GlowButton, GlowInput, LoadingSkeleton, EmptyState, ErrorState, AnimatedList all render correctly
- All components importable from the barrel export at @/components/ui
- Production build passes cleanly with all components
- Animation variants (fadeInUp, stagger) work in EmptyState, ErrorState, AnimatedList
</success_criteria>

<output>
After completion, create `.planning/phases/12-design-system-foundation/12-02-SUMMARY.md`
</output>
